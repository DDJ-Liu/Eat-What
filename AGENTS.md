# 项目开发规范

## 项目状态

这是一个正在积极开发的现有 Unity 项目。

除非明确要求变更，否则应保留现有架构。

## 项目上下文入口

每个新任务开始时，先阅读以下两份项目级文档：

1. `项目整体阅读理解与推进基线_2026-08-14.md`：用于理解当前完成度、产品/架构分歧、风险和推进优先级。
2. `CODEBASE_MAP.md`：用于定位代码入口、调用链、数据/资源依赖、修改影响和验证路径。

随后检查当前 Git 状态，并只补读本次任务涉及的源码和 Unity 资源。无需在每个新任务中重新扫描整个仓库，但不能只凭文档直接修改生产代码。

实际源码、Unity 序列化资源和当前 Editor 状态是最终事实。若文档与实现冲突，以核实后的实际实现为准，并在同一任务中修正文档。

## Unity

Unity 版本：6000.3.24f1（E1已验收；原工作区H导入中，禁止再用2022打开此目录）。

### 活动迁移批次 MIG63（2026-09-21 启动）

用户已确认并启动迁移至 Unity 6000.3.24f1。执行细则见 `.ai-workspace/MIG63/迁移基线.md`，当前状态及人工节点见 `.ai-workspace/MIG63/执行台账.md`；临时限制按 `.ai-workspace/MIG63/临时项登记.md` 登记和解除。

- 原工作区 `D:/GameProject/Eat-What` 使用 `content/p0p1-layout`，已吸收E1候选37bb854，正以6000.3.24f1导入；H通过前继续冻结业务开发。H后第一项为独立定向重序列化，完成前不混入布局修改。
- 独立迁移工作区 `D:/GameProject/Eat-What-U6` 使用 `migration/unity-6.3`；首次 Unity 6 打开由用户执行，不能自动使用其它版本替代。实际导入状态以该副本 ProjectVersion 和台账为准。
- 用户已冻结C=65604ab并确认37bb854人工终验通过；main与unity6.3-baseline已指向该候选。H导入/运行验证尚未闭合，最新入口为 `.ai-workspace/MIG63/E1H_接棒与后续项.md`。Spine两处场景已接Tomato，Renderer延迟启用保留并扩展到Sample；长期工具在 `Assets/Editor/MIG63Validation`，不能作为临时垃圾清理。
- main 仅接收经过验收的集成，不直接开发；`backup/pre-unity6-20260920` 固定保留启动基线。迁移成果在 E1 前不得进入原 2022 工作区。
- 采用手动 MIG- 台账，不启动或复制原自动队列。Spine Sample 与 HorizontalPlayerControllerTest 预研必须保留；不能通过删除或排除其编译完成迁移。
- M4已将MCP收敛为仓库内固定Editor包；服务端来源/锁文件及恢复方式见 `DevTools/MCP/README.md`。使用前每次发现/选择实例并核对Application.dataPath与版本；共享EditorPrefs和外部客户端配置不得盲目改写，不接原自动队列。序列化操作优先MCP/审定Editor API，不手改YAML。
- 用户于2026-09-21人工反馈后批准M1/M2拆分提交和推送，采纳N1在迁移侧跟踪TMP资源；旧Sample/output4难以溯源，Spine测试及后续参考改用已有Tomato 3.8.99，旧数据/场景仍保留。旧数据故障登记为2022已有问题，不再等待重导出阻断E1；Tomato功能回归仍为必要条件。MCP正式依赖采纳仓库内固定包/相对路径方案，M4前记录服务端版本、uv锁文件/来源许可并做6.3连接验证，不搭完整离线服务端镜像。M4已完成最小依赖收敛和连接实测，测试服务在本轮收尾停止；H重新发现目标实例。
- 用户最新决定保留Uniform输入和已验收滚轮修复，明确属于获批交互调整；后续MIG-F01优化统一单位/手感，Mac平台阈值适配必须在Mac使用前验证，不撤销Windows签收。Windows API有意保留D3D11第一、D3D12第二、Auto=false，不误记为DX11-only。
- 两侧字体pre-commit检查保护工作树和index/LFS内容SHA；规则见 `DevTools/GitHooks/README.md`。字体漂移先保全、核对并按Editor恢复流程处理；不得跳过检查误提交，精确哈希放行须有用户批准。该检查与两项手动探针在E2后保留。
- 原工作区接棒验证 H 通过后解除代码冻结；首个 Unity 6 功能批次 F 验收后执行 E2 归档与清理。分支管理独立于工作区清理，不自动删除三条新增分支。

## Unity 工作规则

When working with Unity:

- Prefer Unity MCP for Unity Editor operations such as scenes, GameObjects,
  components, prefabs, materials, ScriptableObjects, editor state, Play Mode,
  Console inspection, and other serialized Unity assets.

- Do not manually edit Unity YAML scene or prefab files when the same operation
  can be performed safely through Unity MCP.

- Source code may be edited directly when appropriate.

- Before modifying existing production assets, inspect the relevant Unity state
  first.

- After meaningful Unity changes:
  1. Refresh/compile Unity if necessary.
  2. Check the Unity Console.
  3. Run relevant tests or Play Mode validation when appropriate.
  4. Review the Git diff.

- Do not commit, revert, discard, or overwrite unrelated user changes unless
  explicitly instructed.

## 通用规则

在修改现有系统之前：

1. 检查相关类及其使用位置。
2. 理解现有实现方式。
3. 优先采用合理范围内最小的改动。
4. 不进行与当前任务无关的重构。
5. 除非明确要求，否则不引入新的架构。

## 项目文档时效维护

关键项目文档必须跟随代码演进维护，不能作为一次性报告冻结。

### 每个开发任务开始时

1. 查看两份项目级文档页首的最后核验日期与核验范围。
2. 检查 Git 状态和本次涉及模块，确认文档中的入口文件、类名、场景和资源路径仍然存在。
3. 若发现代码/资源与文档不一致，先以代码和 Unity 实际状态完成核实，并把文档修正纳入本任务。
4. 若距离上次完整核验超过 14 天，且期间项目仍有代码或资源变化，先进行轻量时效检查：正式 Build 场景、核心目录、主入口类、数据类型和 Resources 路径契约。

### 每个开发任务收尾时

必须进行一次“文档影响判断”：

- 若只是局部实现细节变化，且不影响代码定位、系统边界、完成度或推进判断，可记录为“无项目文档影响”。
- 若触发下列任一条件，应在同一任务中更新对应文档：
  - 核心类、入口、状态机、模块目录或调用链新增、删除、重命名；
  - 数据所有权、跨场景状态、序列化结构或 Resources 路径契约改变；
  - 正式场景、Prefab/ScriptableObject 绑定方式或验证流程改变；
  - 模块完成度、关键风险、架构决策、里程碑或推荐推进顺序改变。

更新目标：

- 结构、入口、依赖和验证方式变化：更新 `CODEBASE_MAP.md`。
- 完成度、风险、决策和路线变化：更新 `项目整体阅读理解与推进基线_2026-08-14.md`。
- 项目级工作规范、Unity 版本或文档维护机制变化：更新 `AGENTS.md`。

为保持新任务入口稳定，日常维护应直接更新上述现有文件及其页首核验信息。除非用户明确要求保留历史快照，不要按新日期反复创建同类文档。

### 定期完整复核

在以下节点执行两份项目级文档的完整一致性复核：

- 一个里程碑完成后；
- 开始批量录入菜谱、步骤或资源前；
- 发生跨模块重构、场景流程调整或数据架构迁移后；
- 活跃开发期间距上次完整核验超过 14 天。

完整复核需要对照源码、Build Settings、关键场景/Prefab/ScriptableObject、Resources 路径和 Git diff。只有实际完成相应核验后，才能更新文档中的“最后核验”日期；不得只改日期而不核对内容。

### 阶段归档与入口简洁化

用户于2026-09-17要求阶段完成后整理AI工作区并保留任务输入/输出。当前历史入口为`.ai-workspace/archive/2026-09-17/README.md`，完整包由Git LFS管理，manifest记录原路径与SHA-256。归档先校验、后清理已核对的历史副本；不得清空活动队列编号、错误史或独立美术服务基线，不移除Unity测试探针和Lab。长期测试夹具应放在`DevTools/*/Fixtures/`，不依赖会被归档的inputs。项目入口文档直接维护当前结论，详细过程记录进入任务输出。归档不代表获得未来提交、推送或新业务触发授权；本次提交/推送/合并按用户本轮明确指令执行。

### 视觉特效组件的配置与使用说明

用户于2026-09-14指定：视觉特效组件必须额外交付面向配置和使用人员的中文说明，统一放在`组件说明文档/视觉特效/`（当前绝对路径`D:/GameProject/Eat-What/组件说明文档/视觉特效/`）。研发文档、源码注释和测试报告不能替代这份使用说明。

- 每个新增视效在同一开发任务中创建相应说明；修改已有视效的功能、可配数值/开关、默认值、单位、参数联动、预设、Inspector入口或适用限制时，同步更新现有说明和目录索引。日常维护直接更新原文档，不按日期重复创建同类说明。
- 按功能组织文档，同一效果涉及多个组件时分节讲清。说明基本功能效果、简单视觉处理逻辑、配置入口/前置条件，以及每个可配置数值和开关对画面表现的影响；给出单位、有效范围、默认值、调大/调小或开关后的变化、联动/覆盖关系和典型配置示例。
- 使用实际Inspector显示名，明确“在哪个对象的哪个组件上改”。区分正式配置入口、自动同步/运行驱动字段、高级接线和诊断项；容易混淆的同名字段、组/本地参数、启停/参与合并等语义必须说明，不能让使用者靠阅读源码猜测。
- 默认值、当前场景样例值和建议值分开标注。依据实际源码、Inspector及资源核对说明；正常遮挡/裁切、性能成本和已知限制用配置人员能理解的语言解释，不把尚未验证的视觉结果写成保证。
- 每份说明记录最后实际核对日期和范围，并在该目录README中登记。开发任务收尾列出说明路径及本次更新点；没有涉及说明内容变化时给出简短的文档影响判断，不能只改核验日期。
- 现有泡泡视效和描边/阴影的首批说明由VFX-DOC-T1补齐。此规则是交付/维护要求，不改变用户指定的手动或监管模式，不自动触发任务，也不新增中途人工审批要求。

## Unity 资源安全

### MCP验证产生的临时变化

用户于2026-09-15明确：流水线自身操作产生的脏场景应由流水线自主恢复清理，不重复提交人工审查。执行前记录干净/已有变更基线、已保存恢复点和临时操作归属；经核实仅含本任务临时变化时，使用Unity Editor/MCP恢复原值或重载已保存成果，清理本任务临时资产并复核diff/Console/资源释放。不得仅凭dirty标记熔断，也不得用MarkSceneClean掩盖未知变化或保存覆盖用户未保存成果。完整流程见`.ai-workspace/UNITY_SCENE_CLEANUP.md`；实际平台拒绝仍依返回原因处理，不绕过安全限制。

### 美术资产比对、命名与导入顺序

用户于2026-09-15明确：资产导入工程任务应先识别根目录`美术资产/`同步区的实际内容，逐项与工程现有资产比对，提交最终存放路径、命名修改、复用/替换/新增/暂缓处置和后续配置草案，经用户审阅后才导入。不得默认先整体搬入`Assets/ArtImports`或其它中转目录再开始识别和规划。

- NAS到同步区的同步与Unity工程导入是两个阶段；ART负责同步、内容与差异报告，ENGINE_MCP先只读核对工程资产、GUID、引用、Importer和加载路径契约，再由控制台汇总审批。
- 草案逐文件列源路径/哈希、工程现状、建议最终路径/名称、变更原因、预期使用点、覆盖与引用影响、备份/回退；同图优先复用，身份未明或未来用途素材明确暂缓，不自动绑定。
- 审批后直接导入审定的正式目录。已有工程资产迁移优先用AssetDatabase.MoveAsset保留GUID；替换前检查引用和备份。只有用户明确要求时才建立临时导入目录，且同时约定清理边界。
- 只执行已审范围；源文件版本/用途/路径范围发生实质变化时重新提交差异。收尾提供实际路径/名称/引用、导入验证与配置结果，不把文件复制完成当作场景配置完成。此规则不自动改变手动或监管模式。

除非确有必要，否则不要直接编辑 Unity YAML 文件。

这包括：

- `.unity`
- `.prefab`
- `.asset`
- `.meta`

修改资源时，优先使用 Unity 编辑器 API 或支持 Unity 的工具。

## 序列化

修改以下内容时务必谨慎：

- 序列化字段名称
- 公开的序列化数据
- `ScriptableObject` 结构
- 存档数据结构

除非明确要求，否则应保持向后兼容。

## Git

确保改动聚焦且便于审查。

除非明确要求，否则不要提交或推送代码。

## 验证

### 视觉效果的统一试验入口

用户于 2026-09-09 指定：今后新增或修改 Shader、折射/形变、描边、阴影、粒子等视觉效果，先在独立 `Assets/Scenes/ToolTests/VisualEffectsLab.unity` 场景完成可重复测试与人工视觉验收，再另行安排正式场景接入。该路径为统一入口；首次建设尚未完成时不得声称已有可用场景。

- 先交付一套完整效果试样、可操作演示、开关/参数对照和验收路径；不把纯静态编译或探针通过当作视觉验收。
- 不为每个新效果另建平行 Lab 场景；在统一场景中按具名功能组扩充，保留既有样例与回归能力。
- 未经明确授权，不因 Lab 测试改动正式场景、共享材质或全局渲染设置，不修改正式 Build 场景列表。
- 场景接线/静态审计和运行验证分阶段记录，人工闸门、脏场景保护及既有流水线职责保持不变。

完成代码修改后：

- 检查是否存在明显的 Unity 编译问题。
- 报告发生改动的文件。
- 说明仍需在 Unity 内完成的验证事项。
