# 做菜短循环设计文档与当前代码评估

## 报告信息

- 整理时间：2026-08-11 19:10:51（Asia/Shanghai）
- 项目：`Eat-WhatProject`
- Unity 版本：`2022.3.62f2`
- 评估性质：设计文档与当前仓库的静态对照评估
- 本轮操作：仅整理并新增本报告，未修改 Unity 代码、场景、Prefab、资源或配置

## 设计依据

1. `D:/GameProject/养分合作proj/Design/《是啊，吃什么》F2-a_短循环UI框架_v0.5.3.docx`
2. `D:/GameProject/养分合作proj/Design/《是啊，吃什么》B1_短循环系统设计_v2.0.docx`

本轮已结合两份文档共 72 页内容，对当前做菜短循环相关代码、场景、Prefab、ScriptableObject 和配置关系进行了检查。主要设计范围包括：

- B1 第 2～3 页：短循环主流程与存档点
- B1 第 4～17 页：阶段 0、阶段 1
- B1 第 18～24 页：阶段 2
- B1 第 25～28 页：阶段 3、阶段 4 与数据需求
- F2-a 第 5～23 页：阶段 0、阶段 1 的界面框架
- F2-a 第 24～36 页：阶段 2 的界面框架
- F2-a 第 37～39 页：阶段 2.5、阶段 3 与跨界面关系

## 一、总体结论

当前项目已经具备阶段 0～2 的部分场景、状态切换、拖放交互和番茄炒蛋原型，但尚未形成设计文档定义的完整做菜短循环。

现阶段最关键的问题并不是“还缺多少 UI”，而是阶段 2 的核心数据模型与 B1 v2.0 的设计方向存在明显差异：

- 设计要求：全局步骤表、菜谱步骤配置、运行时动态完成与永久完成状态；玩家原则上可使用全局步骤，不由菜谱白名单限制。
- 当前实现：`Recipe` 内维护步骤白名单，容器和工具校验依赖白名单，步骤解析也从菜谱白名单中查找。

如果 B1 v2.0 是后续正式实现基线，这部分需要先确认口径，否则继续补充界面、关卡和新菜谱时会在现有结构上积累返工成本。

## 二、完成度概览

| 模块 | 当前判断 | 说明 |
| --- | --- | --- |
| 阶段 0：菜谱确认 | 部分完成 | 有菜谱入口、翻页和选择原型，但数据驱动、锁定、收藏、动态详情等不完整 |
| 阶段 1：备菜 | 部分完成 | 有冰箱、食材拖放和托盘状态，但容量、分类、堆叠、库存扣减等仍是原型 |
| 阶段 1 → 阶段 2 上下文 | 高风险 | 所选菜谱与食材状态没有形成可靠、统一的跨场景传递链路 |
| 阶段 2：厨房框架 | 部分完成 | 有房间选择、食材选择、交互状态和两个加工小游戏 |
| 阶段 2：步骤系统 | 与设计不一致 | 当前以菜谱白名单为核心，和 B1 v2.0 的全局步骤模型冲突 |
| 必备物处理 | 原型阶段 | 有普通材料判定和弹窗雏形，但非通用、缺少完整交互规则 |
| 硬做菜 | 当前不可正常到达 | 错误计数和强制产物链路被前置校验截断，场景配置也不完整 |
| 阶段 2.5 | 未发现实现 | 完整版需求，当前不一定是 Demo 阻塞项 |
| 阶段 3、阶段 4 | 未发现完整实现 | 若当前目标覆盖完整 Demo，则属于关键缺口 |
| 存档点与流程恢复 | 未发现完整实现 | 文档中的存档点 1、2、3 尚未形成明确架构 |

## 三、阶段 0：菜谱确认

### 已建立的部分

- `CookingPrepare` 中存在自由做菜入口。
- `RecipeBookManager` 已覆盖菜单、菜谱页、翻页和菜谱选择等基础行为。
- 当前菜谱详情主要通过固定 Prefab 展示。
- `CustomerManager` 与对话相关代码体现了顾客点单的原型方向。
- 准备阶段已经存在菜单、菜谱、冰箱等状态切换框架。

### 与设计文档的主要差距

- `CustomerManager` 所在对象当前处于禁用状态，默认流程更接近自由做菜；`isFreeCooking` 默认为 `true`。
- 菜谱名称、标签、所需食材、替代食材、标准步骤等内容尚未形成完整的数据驱动展示。
- 详情页依赖 `Resources/Prepare/RecipePage/{Recipe.name}` 下的人工 Prefab，新增菜谱需要同步维护资源命名和页面内容。
- `RecipeBookButton` 主要设置主图标，没有看到文档要求的完整卡片信息绑定。
- 未发现收藏、菜单排序字段、未解锁菜谱表现、动态线索板等完整实现。
- 未发现不同来源进入菜谱页后对应的退出路径差异。
- 未发现设计中的存档点 1。

### 判断

阶段 0 可以支持演示性入口和固定菜谱浏览，但还不是文档所描述的可扩展菜谱系统。

## 四、阶段 1：备菜与冰箱

### 已建立的部分

- 与阶段 0 共用场景和摄像机状态切换。
- 冰箱管理器具备分段展示和滚动行为。
- 食材可以拖动到托盘。
- 托盘存在 `Idle`、`Highlight`、`Opened` 等状态。
- 可见托盘物品能够同步到 `ingredientsInTray`。
- `IngredientInventory` 和 `SceneSwitchManager` 使用了 `DontDestroyOnLoad`，说明项目已经尝试保留跨场景状态。

### 与设计文档的主要差距

- 当前托盘仅配置约 5 个场景位置，未达到文档所述的 20 格基础容量，也未实现 30 → 60 的升级容量。
- 当前库存主要是番茄、鸡蛋等测试数据，且数量接近固定测试值。
- 未发现 `Single`、`Stack`、`Container`、`Pack` 等库存形态的完整建模。
- 未发现一键整理、分类筛选、排序字段、猫身体长度与容量关联、空位占位等完整功能。
- 未发现容器或包装取用后的剩余状态处理。
- 未发现完整的 `0/20` 等容量显示与限制反馈。

### 库存扣减问题

`SyncIngredientsFromTray` 当前更接近“根据场景物品重建托盘列表”，没有形成明确、统一的库存扣减事务。部分加入托盘的代码路径仍有注释或未闭合逻辑；进入做菜后生成食材时，也没有看到可靠的托盘数量扣减。

这一点需要结合设计口径进一步确认：库存究竟在“放入托盘”“切换到厨房”还是“实际投入某一步骤”时扣减，并确保所有入口使用同一规则。

## 五、阶段 1 → 阶段 2：菜谱与上下文传递

这是当前最值得优先核实的工程风险之一。

- `PrepareIngredientManager.currentRecipe` 没有看到可靠的跨场景持久化或复制流程。
- `CookingManager` 本身没有看到与准备阶段相同的持久化方式。
- `CookingProcess` 仍绑定固定的番茄炒蛋资源。
- 准备场景和加工场景可能各自绑定不同的 `Recipe` 资源实例。
- `PrepareIngredientManager` 中仍保留相关 TODO，说明该链路尚未正式完成。

因此，从菜谱页选择的菜谱、托盘中的食材以及进入厨房后的当前菜谱，暂时不能确认会始终指向同一份运行时上下文。

建议后续首先用 Unity Play Mode 验证：选择不同菜谱后进入厨房，`currentRecipe`、托盘内容和步骤判定是否同步变化。

## 六、阶段 2：厨房、步骤与加工

### 已建立的部分

- 存在 `KitchenChoiceState`、`IngredientChooseState`、`InteractionState` 等状态。
- `CookingRoomManager`、`Container`、`CookingDropZone` 已建立房间、容器和放置区域的基本关系。
- `InteractionController` 提供了小游戏交互入口。
- 已实现或接入切丁、搅拌等加工交互。
- 加工产物能够加入库存，并清理当前加工区。

### 与设计文档的主要差距

- 当前场景只看到约 2 个厨房房间或对应数据，未达到文档中初始 3 个、后续可扩展至 6～8 个的目标。
- 未发现房间解锁、空位占位和扩建的完整机制。
- 未发现由数据驱动的顶部步骤栏，以及步骤的动态完成、永久完成状态表现。
- `CookTableManager` 中仍有房间或操作台加载相关 TODO。

## 七、阶段 2 核心数据架构与 B1 v2.0 的冲突

### B1 v2.0 的方向

- 建立全局步骤表。
- 菜谱只配置标准步骤、触发条件与变体关系。
- 玩家原则上可以尝试全局步骤，而不是只能执行菜谱白名单中的步骤。
- 步骤需要支持运行时动态完成和永久完成。
- “硬做”产生的物品应作为普通产物进入库存并参与后续流程。

### 当前代码方向

- `Recipe` 中包含 `whitelistRules`、`allowedToolTags` 等白名单数据。
- `CookingDropZone` 会拒绝白名单之外的食材或工具。
- `CookingManager.ResolveRule` 主要从菜谱白名单中解析步骤。
- 容器标签校验也依赖菜谱白名单。
- `StepOutputRule` 尚未看到完整使用链路。
- `RecipeVariant` 当前基本为空，尚未承载变体数据。

### 判断

这不是局部功能缺失，而是核心规则建模不同。若直接在现有白名单结构上继续增加菜谱和步骤，后续实现自由尝试、错误组合、隐藏变体和硬做菜时会受到较大限制。

在修改代码前，应先由设计与程序共同确认：B1 v2.0 是否正式废弃菜谱步骤白名单。

## 八、硬做菜链路

当前硬做菜逻辑存在不可到达风险：

1. `CookingDropZone.TryAcceptTool` 先调用 `CanAcceptTool`。
2. 无效工具会在前置校验阶段直接被拒绝。
3. 错误次数统计位于 `CookingManager.onToolPlacedIn` 的无效分支。
4. 因为无效工具通常到不了该分支，所以通过多次错误触发硬做的正常路径被截断。

此外，场景中的 `forcedCookingRule` 和 `unknownObjectOutput` 没有看到完整配置。因此即使绕过前置问题，也不能确认能够生成设计要求的未知或强制产物。

### 判断

硬做菜目前不应视为“已完成但待调参”，而应视为触发规则、状态流和资源配置均需补全的功能。

## 九、必备物与普通材料

### 已建立的部分

- `IngredientData.isCommonMaterial` 提供了普通材料标记。
- 代码能够派生普通材料和非普通材料列表。
- 已存在必备物提示数量计算的雏形。
- `CommonMaterialPopupManager` 固定处理水、油、面粉等材料。
- 部分普通材料可以走旁路接收逻辑。

### 主要缺口

- 普通材料尚未形成真正独立、通用的数据分类和动态列表。
- 弹窗内容和图标更接近固定实现。
- 未完整区分必选与可选材料。
- 未看到取消、继续等完整交互闭环。
- 未看到材料数量上限和消耗规则。
- 当前番茄炒蛋的最后翻炒步骤未进入有效菜谱规则，可能导致该弹窗流程无法按设计触发。

## 十、当前番茄炒蛋 Demo 的实际闭环

当前番茄炒蛋菜谱白名单只包含类似以下步骤：

- 番茄 → 切丁
- 鸡蛋 → 搅拌

规则库中虽然存在番茄与鸡蛋的最终翻炒规则，但它没有被当前菜谱白名单纳入。因此静态检查能够确认的闭环只有两个独立加工步骤，最终成菜步骤不可正常访问。

即使强行执行最终规则，`CookingManager` 的 `isFinal` 分支目前也更接近注释或占位，没有形成到阶段 3、阶段 4或结果页的正式跳转。

## 十一、阶段 2.5、阶段 3 与阶段 4

### 阶段 2.5

未发现完整实现。根据文档定位，它更偏向完整版扩展内容，因此不一定阻塞当前最小 Demo，但需要明确排期边界。

### 阶段 3、阶段 4

未发现以下关键能力的完整实现：

- 还原度、味道、外观等三维评分。
- 工具或操作对评分的系数影响。
- 中间产物的评分继承。
- 最终成品外观和变体名称生成。
- 特殊组合与隐藏变体。
- 变体保存、图鉴写入和库存回写。
- 成品耐久度或后续使用状态。
- 将本轮结果传递给上游经营或角色系统。

`IngredientData` 中能看到味道、外观等字段雏形，但目前未见实际评分链路；还原度字段和完整计算模型也未建立。`RecipeVariant` 当前没有承担文档要求的变体结构。

如果当前 Demo 目标包含“完成一道菜并展示结算”，阶段 3、阶段 4属于关键阻塞项；如果 Demo 只验证切丁和搅拌小游戏，则需在里程碑说明中明确范围。

## 十二、其他静态检查发现

1. `RecipeBookRecipeState` 的下一页边界可能允许索引等于列表数量，存在越界风险。
2. 托盘满时，`AddItem` 可能直接返回，但调用方仍继续执行 `ingredient.onPlaceRight`，可能造成视觉状态与数据状态不一致。
3. 切换菜谱时会清理数据侧的 `ingredientsInTray`，但未确认同步清理场景中可见的 `trayItems`。
4. `CookTableManager` 的房间或操作台加载逻辑仍有 TODO；若当前只是 Demo，可暂时使用假界面，但应明确它不是正式数据链路。
5. 阶段 2 的库存更接近按食材类型聚合，数量显示与实际扣减没有形成完整闭环。
6. 未发现文档中存档点 1、2、3 的统一实现与恢复流程。

## 十三、建议的后续核查优先级

在不重构、不修改代码的前提下，下一轮最值得依次确认的问题是：

1. B1 v2.0 是否为正式实现基线，菜谱白名单是否确定废弃。
2. 当前 Demo 的验收终点是两个加工小游戏，还是必须覆盖最终成菜与阶段 3、阶段 4。
3. 阶段 1 → 阶段 2 应传递哪些运行时上下文，以及存档点如何落地。
4. 库存扣减的唯一时机：放入托盘、进入厨房，还是实际执行步骤。
5. 硬做菜的正式触发条件，不应继续依赖当前不可到达的“连续错误工具”分支。
6. 当前两个厨房房间是临时 Demo 配置，还是与文档初始三个房间的要求发生了变更。
7. 哪些 UI 允许继续使用人工 Prefab，哪些必须改为由菜谱和步骤数据动态生成。

## 十四、可信度与仍不确定的事项

### 可以较高置信度确认

- 当前代码和资源已经搭建阶段 0～2 的原型骨架。
- 番茄炒蛋尚未形成最终成菜闭环。
- 阶段 2 白名单模型与 B1 v2.0 的全局步骤设计存在实质差异。
- 硬做菜的错误触发链路在静态代码上存在不可到达问题。
- 阶段 3、阶段 4和三个存档点尚未形成完整实现。

### 仍需在 Unity 中确认

- 当前工程是否能够无错误编译。
- 场景中的序列化引用和 UnityEvent 是否全部有效。
- 被禁用对象是否会由其他入口在运行时启用。
- 固定 Prefab 中是否通过静态内容补充了代码未体现的界面信息。
- 不同菜谱选择后跨场景数据是否在实际运行中发生丢失或覆盖。
- 切丁、搅拌、托盘满载、普通材料弹窗和最终加工的实际交互表现。

## 十五、本报告边界

本报告是基于设计文档、代码和可见 Unity 资源关系得出的静态结论，尚未执行 Unity Play Mode、正式编译或完整关卡回归。报告中的“未发现”表示在当前检查范围内没有建立可确认的完整实现，不等同于断言项目中绝对不存在任何相关占位内容。

