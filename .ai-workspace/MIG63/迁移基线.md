# MIG63 · Unity 6.3 迁移基线 v1.2（已确认执行版）

日期：2026-09-21。状态：M1/M2已分开提交并推送；用户完成初验、VFXLab和Windows人工跑测，慢速滚轮回归正在闭合；Spine改以Tomato 3.8.99为基准，N1已纳入TMP版本管理。目标仍为Unity 6000.3.24f1。最新依据为同目录[M3R人工反馈处理与补漏](M3R_人工反馈处理与补漏.md)及执行台账；尚未进入E1。
依据：用户本轮四项决定、v1.1、设计端反馈，以及开发端 2026-09-20/21 的源码、Git、Unity 只读核验。
用户已于2026-09-21确认启动，随后以“按清单推进”批准M1修复范围，按本稿逐步执行，不再并用v1.1失效条目。首次Unity 6打开及M1范围决策已完成人工节点，最终SHA/视觉验收仍保留；本批次不自动启动原队列或新业务。
已确认的四项决定不再反复询问；保留首次 Unity 6 打开、Spike 后范围决策、视觉验收和用户本人批准 SHA 等具体人工节点。

## 1. 已确认决定与执行边界

1. 目标为 Unity 6.3 LTS；M0 核定一个具体 6000.3.x 补丁并锁定，批次内不自动追新。保留 Cinemachine 2.x，本批次不顺带切 CM3、输入架构、程序集架构或官方 AI 工具链。
2. Spine 预研必须保留，其功能未来会用于长循环。不能因当前产品场景未引用，就删除 runtime、示例、角色资产或相关代码，也不能把排除编译当作迁移成功。
3. 在 migration 分支通过 Unity Editor/API 执行 Build 调整：ShortCycle_P0P1 为启动入口，CookingPrepare 移出 Build，Windows x64 为本轮 Player 验收目标。CookingProcess 保留现状；旧业务链不扩展为本轮运行验收范围。
4. 分支继续使用 content/p0p1-layout 与 migration/unity-6.3，不改为 develop。它们按阶段开发、关键节点验收和合并管理，不定义为永久开发分支，也不在 E2 自动删除。
5. 额外从 main 创建 backup 分支；三条分支均推送并建立正确 upstream。main 不直接开发，backup 不接收后续开发提交。
6. 双线结束必须有明确条件、解除记录和清理链路。先归档校验，再清理；不无限期保留迁移副本。
7. 所有执行变更在已建立的内容/迁移分支完成；main 不直接开发。

## 2. 当前已核实事实及限制

- S 基线：ec3aa7c6f3d12d5eeb8ebbaa561d227b174107d0；2026-09-21 启动前已重新查询实时远端 main，与本地一致，工作树干净。原工作区现已切到 content/p0p1-layout。
- 当前原工程 Unity 2022.3.62f2；M0 已执行五场景 Play 对照和既有离线回归。迁移副本已由用户用6000.3.24f1首次打开；M2已解除URP旧Compatibility构建阻断，Unity 6场景机器回归及Windows构建/启动冒烟通过。人工视觉/真实输入与Spine仍待闭合，详见同目录M2M3执行结果。
- 业务主要在 Assembly-CSharp / Assembly-CSharp-Editor；Spine、Febucci 有独立 asmdef。不能说所有第三方都在同一程序集，也不能把独立 asmdef 当作编译故障天然隔离。
- Renderer2D 的 Renderer Features 为空。自制描边合并用 beginCameraRendering/CommandBuffer，背景捕获用独立 Camera/RT。相关自制渲染目录本轮未检出 RenderSingleCamera、SubmitRenderRequest 或显式 Camera.Render 调用；仍需检查 URP 17 下时序、混合和 RT 生命周期。
- 导表输入位于 D:/GameProject/Eat-What/DataTables；输出位于 D:/GameProject/Eat-What/Assets/Generated/DataTables，产物为 .asset，导入器更新/创建资源并保留引用契约，不生成业务 .cs。
- 字体资产当前含 216 个字符记录。这是盘点值，不是永远不变的验收常数；TMP 数量必须按相同内容和相同运行状态比较。
- D:/GameProject/Eat-What/Assets/TextMesh Pro 的内容被忽略；D:/GameProject/Eat-What/Assets/Spine Examples 也被忽略。备份分支不能单独覆盖这些文件、外部工具和 LFS 对象。
- 当前队列是 MANUAL、自动链 PAUSED_MODE、监管 COMPLETE。维持现状；独立日审也保持原配置。本批次采用手动 MIG- 台账，不复制、启动旧控制台，不占用或重置原 AI 编号。

### 2.1 Spine 专项核验

使用 Unity AssetDatabase.GetDependencies 对 D:/GameProject/Eat-What/Assets/Scenes 下 15 个场景查询递归资源依赖；该查询未打开或保存场景。完整结果见 D:/GameProject/Eat-What/.ai-workspace/outputs/control/MIG63_迁移规划_20260921/Spine场景依赖只读核验.json；后续 M0 的实际 Play 结果另见执行台账。

| 场景 | 核实结果 |
| --- | --- |
| D:/GameProject/Eat-What/Assets/Scenes/Spine Sample.unity | 引用 Spine runtime、Sample/O4 角色、SkeletonMecanim、CharacterEquipment、CharacterTestController 等 |
| D:/GameProject/Eat-What/Assets/Scenes/ToolTests/HorizontalPlayerControllerTest.unity | 同样引用 Spine；包含 Sample.prefab、HorizontalPlayerController 和 SpineRuntimeMeshRendererBootstrap |
| D:/GameProject/Eat-What/Assets/Scenes/Cooking/ShortCycle_P0P1.unity | 本次资源依赖查询未发现 Spine |
| D:/GameProject/Eat-What/Assets/Scenes/ToolTests/VisualEffectsLab.unity | 本次资源依赖查询未发现 Spine |
| D:/GameProject/Eat-What/Assets/Scenes/ToolTests/FridgeScrollLab.unity | 本次资源依赖查询未发现 Spine |

源码直接使用 Spine 的业务类至少包括：
- D:/GameProject/Eat-What/Assets/Scripts/Spine_Package/Scripts/CharacterEquipment.cs。
- D:/GameProject/Eat-What/Assets/Scripts/Controller/HorizontalPlayerController.cs。
- CharacterTestController 通过 CharacterEquipment 间接依赖。
- SpineRuntimeMeshRendererBootstrap 不直接使用 Spine 类型，但承担测试场景的 MeshRenderer 延迟启用逻辑，必须保留并验证。

HorizontalPlayerControllerTest 当前 spineAnimation 字段为空，实际使用 Sample 的 SkeletonMecanim + Animator，配置 idle/walk 状态切换。不能仅测试 SkeletonAnimation 路径就宣称现有移动预研通过。
查询不穷尽运行时字符串加载和所有未来使用场景；资产是否参与打包不能仅靠上述结果判断。Spine Examples 未入库，不能整体复制或整体抛弃，须以实际依赖和预研恢复需要确定清单。
初始盘点在Assets和根美术同步区未检出.spine文件。用户最新反馈更正：旧Sample/output4难以溯源，切换为已有Tomato 3.8.99测试，今后以此参考。旧素材保留；不再把两组旧素材重导出作为等待项。M0运行故障及最新路线见执行台账MIG-P01和M3R报告。

### 2.2 Spine 处理路线

默认保留现有3.8 runtime、旧数据和两处预研。M0已确认旧3.8.75故障在2022存在，登记历史缺陷并解除E1等待旧素材重导出的条件；按照用户新决定，复用Assets/Scripts/Spine_Package/Avatars/Tomato的3.8.99作为当前与后续参考。
迁移回归仍必须覆盖角色显示、左右移动/停止/转向、idle/walk/kick、换装、SkeletonAnimation/SkeletonMecanim、重复进退Play和既有Renderer延迟启用能力。当前可用临时运行对象验证，正式参考绑定在M4随冻结内容通过Editor API处理；不能仅以GetSkeletonData成功声明功能通过。未来如需替换旧文件，按CONTENT资产审查及导入路径处理。
本轮验收保护现有能力，不扩展实现尚未完成的长循环。
若 3.8 无法以合理成本保留，M1 输出：具体阻断、升级涉及的数据和代码、源工程/导出能力、预计成本，由用户决定升级范围或暂缓整个迁移。仅为诊断而隔离插件的副本不能作为 E1 成果。
改用 4.2 属于条件路线：官方支持表覆盖 Unity 6000.3，但 runtime 与导出数据版本仍须匹配；先核实源工程和重新导出能力，再制定插件及 Shader 的配套升级方案。不得直接覆盖 3.8 数据。

## 3. 工作区、分支和引用

| 角色 | 位置 | 分支/upstream |
| --- | --- | --- |
| CONTENT | D:/GameProject/Eat-What | content/p0p1-layout → origin/content/p0p1-layout |
| MIG | D:/GameProject/Eat-What-U6（M0 已独立克隆） | migration/unity-6.3 → origin/migration/unity-6.3 |
| BACKUP | 不需常驻 Editor | backup/pre-unity6-20260920 → origin/backup/pre-unity6-20260920 |
| MAIN | 集成目标 | main → origin/main；不直接开发 |

沿用上一轮提出的 backup 名称；名称中的日期不替代实际 S 时间和提交记录。三条新分支均从 S 时点核验后的同一个 main SHA 创建。
推送后核对 local branch、upstream、远端实际 ref 和 SHA；不能只检查本地 origin 缓存。已有 DDJ_260817 保持原状。
pre-unity6-baseline 标签锚定 S；unity6.3-baseline 锚定最终已验收集成成果；不移动已有同名标签。若启动时已存在同名分支/标签，核实来源，不覆盖。
合并方向：content → migration → main。E1 前禁止 migration 成果进入原 2022 工作区；不把迁移提交直接 checkout 到 CONTENT。
原则上 main 在迁移期间保持 S；如果实际 main 有新提交，先重核差异和集成结果，不强推覆盖。
backup 长期保存；开发和迁移分支在各自阶段完成后由用户按习惯决定退役，不因 E2 清理自动删除。
回退依赖“Git 锚点 + 未入库资产快照 + LFS 可用对象 + 外部工具版本/配置记录”，不能只验分支存在。

## 4. 文件归属和合并规则

以下路径以各自 clone 根目录为基准，操作前展开并核对绝对路径。

| 内容 | 并行期归属/纪律 |
| --- | --- |
| 业务 .unity / .prefab，布局和已有组件接线 | CONTENT；MIG 在最终内容冻结前不提交业务场景/Prefab 的保存结果 |
| DataTables CSV、对应 Assets/Generated/DataTables 产物及 .meta | CONTENT 成对交付，先在 2022 导入验证；schema/导入器代码变化另列代码范围 |
| ProjectSettings、Packages 清单/锁文件、必要 .cs、Shader、渲染设置、迁移依赖 | MIG；不擅自改业务含义 |
| TMP 字体、材质 | 原则 MIG；内容侧新增字形必须登记，最终保留内容所需字符、GUID 和回退链，再在 6.3 验证 |
| 新美术资产及 .meta | CONTENT，沿用工程已有逐文件审查导入规则 |
| 第三方升级自身的场景/Prefab/.meta | M1 若确需升级，列出精确目录作为例外，不扩大为业务场景可随意保存 |
| 根入口文档与现行工作流文件 | 并行期 CONTENT 维护；MIG 留增量说明，汇合时纳入最终成果；不在 main 直接改 |
| 迁移诊断脚本及其 .meta | 经 M1 修复清单明确归 MIG；临时/长期性质、移除节点逐项登记 |

生成资产冲突优先以已冻结的 CONTENT 数据为业务依据，但先检查是否同时存在 schema、GUID、字段或导入器变更；不能整目录删除后重建。
在 6.3 使用 Tools > Data Tables > Import All 更新并比较语义、GUID、引用、创建/更新/未变清单，完成后再跑验收。二次导入不应产生无法解释的业务变化。

业务场景/Prefab 若发生冲突：先保存两侧证据并识别来源。已证明 MIG 只有可重现的自动升级、没有独有获批人工接线时，整份以 CONTENT 为输入，再由 Unity 6 升级；若存在独有修改，在 Editor 中有序重做并核验，不直接丢弃。
不人工拼接 Unity YAML；不使用“文件类型决定全部取 ours/theirs”的通用规则。
脏场景只恢复有记录、可归属的本任务临时变化，遵循 D:/GameProject/Eat-What/.ai-workspace/UNITY_SCENE_CLEANUP.md。未知变化不得用 checkout、强制保存或 MarkSceneClean 掩盖。

## 5. 工具隔离和验证方式

迁移侧默认不建立第二 MCP server/控制台，采用人工 Editor + batchmode/日志 + 现有离线回归；优先减少本批次工具改造。
用户本轮采纳正式依赖方案b：仓库内固定版本包、相对manifest路径；M4前同时处理服务端源码/运行依赖、版本与许可、共享配置隔离和6000.3连接实测。完成前继续隔离，不把一个Editor包副本当作完整离线恢复，不启动原自动队列。
“不挂 MCP”不自动保证 Editor 插件不自启。M0 在迁移副本首次打开前核实两个现有连接器的初始化逻辑、自动连接与共享配置写入；如需禁用/移除副本中的连接器入口或改 manifest，先形成精确配置差异，作为启动准备的已审范围执行。原工作区和共享外部插件目录不作并行试验。
若没有可靠的项目级隔离方式，保持顺序操作并解决具体配置问题，不以改共享 EditorPrefs 地址来假装双侧独立。
ProjectSettings/manifest 的准备性修改与首次导入后的修复均限 MIG；M1 记录自动变化和人工变化的区别。
batchmode 运行前关闭同一路径的交互 Editor；固定引擎绝对路径、projectPath、日志路径、退出码与完成标记。不把 -nographics 编译通过当作 GPU 视觉通过，也不假定当前存在完整 NUnit 测试程序集。
EditorPrefs 在 Windows 同一用户的注册表配置中共享，并非所有 Windows 用户共用。自有工具本轮查到：
- SpriteButtonVisual_FolderPath / SpriteButtonVisual_Suffix_*。
- ImageButtonVisual_FolderPath / ImageButtonVisual_Suffix_*。
- DataEncryptor_ExcelFolderPath。
- CommandBinding_SelectedAssetGuid。
- EatWhat.CameraFrames.Show。
迁移期记录相关原值，避免从 MIG 重写指向 CONTENT 的绝对目录。只恢复本批次实际改动项，不整体恢复注册表或客户端配置，不删除用户期间新增设置。
MCP 外部包路径的临时隔离写法必须在最终验收前收敛为原工作区和可恢复环境均能解析的确定依赖；同时验证 package lock。不能把指向待删除迁移目录的 file: 路径合入 main。
如果最终采用嵌入/本地包，需检查 .gitignore 对 Packages 的排除规则和应入库文件，避免“本机可用、clone 缺包”。

## 6. 构建入口与验证边界

MIG 的 D:/GameProject/Eat-What-U6/ProjectSettings/EditorBuildSettings.asset 通过 Unity Editor/API 更新，不手改 YAML：
- index 0：Assets/Scenes/Cooking/ShortCycle_P0P1.unity，启用。
- CookingPrepare：从 Build 列表移除，场景文件保留。
- CookingProcess：保留启用，后移到 index 1；不借此次迁移删除旧代码/场景，也不声明其旧业务链已完整运行验收。
- 两个 Lab、Spine Sample、HorizontalPlayerControllerTest 不加入正式列表。
如采用 6.3 Build Profile，核对它实际使用的场景列表与上述目标一致，不能只改旧列表后忽略 profile override。
Windows x64 Player 必须从 ShortCycle 启动并完成本轮约定操作；Spine 两处预研至少在 6.3 Editor 验证。若涉及其 runtime/Shader 修复，补独立诊断 Player（显式场景清单，不永久改变正式列表）。
Build 改动已在M2通过Editor API完成：迁移副本ShortCycle index0、Process index1，当前Profile不覆盖列表；原2022工作区尚未接棒。

## 7. 生命周期和关键节点链路

REVIEW → S/M0 → M1 → M2/M3（与内容并行）→ C 冻结 → M4 → E1 → H 接棒 → F 首个功能批次闭合 → E2 清理关闭。

| 节点 | 进入条件与操作 | 离开证据 / 人工介入 |
| --- | --- | --- |
| REVIEW（已完成） | 用户四项决定已吸收，输出本审查稿；不动工程 | 用户已于2026-09-21确认启动本稿准备范围 |
| S / M0 | 重新核对 Git/Editor；固定基线和版本；创建推送三分支及 pre 标签；完整恢复清单、手动台账与临时项登记；clone 和工具隔离 | upstream/远端 SHA、快照哈希、依赖恢复核对、路由和状态检查。新目录权限在执行时落实 |
| M1 | 用户首次用锁定 6.3 打开 MIG；记录全部自动变更、编译错误、依赖/渲染/Spine 问题；先测现有实现 | 用户审查具体修复范围与 Go/No-Go；初估工期更新 |
| M2 / M3 | 仅在 MIG 进行必要修复、编译、离线验证；VisualEffectsLab 视觉、Spine 两处预研复核；内容侧保持范围内调整 | 机器结果和人工初验，问题分类；不以临时宏/排除功能封口 |
| C 内容冻结 | CONTENT 完成布局和生成数据；在 2022 做归因验证，固定内容提交 C | 记录同一 C 的场景、运行状态、输入步骤、字体、截图和相关性能；这是最终对照基线 |
| M4 汇合 | CONTENT C → MIG；解决冲突；完成 Build、Tag/Layer、字体、导表、依赖路径和全部必要资源升级/保存；汇入文档增量 | 重新跑完整最终回归，移除过渡宏/临时诊断，形成最终树和场景哈希 |
| E1 技术验收 | 用户本人确认最终 SHA、场景基线和视觉/交互结果；之后合入 main、打 unity6.3-baseline 并推送 | 最终内容、包、源码、Build、文档与验收对应；若合并改变树，须重验后再打标签 |
| H 原工作区接棒 | 关闭原 2022 Editor；CONTENT 分支吸收已验收成果；恢复升级后的本地 TMP/依赖；用同一 6.3 打开并回归 | 接棒通过后解除内容代码冻结、迁移文件独占等阻碍功能开发的限制；记录解除差异 |
| F 首个功能批次 | 用户另行指定具体范围与验收，不由 MIG63 自动新建业务；在开发分支开发、按关键节点验收合并 | 批次验收通过且接棒环境稳定；记录该批次 ID/提交和人工验收 |
| E2 关闭 | F 通过且恢复包、迁移证据、未完事项与临时项处理齐全 | 先归档校验，再删除迁移副本和关联临时配置；用户确认关闭记录 |

关键顺序：先完成所有影响验收的变更，再最终测试，再用户批准 SHA，再集成/标签。禁止在 E1 后才补“本应参与最终验收”的导表或 TagManager。
初始 2022 盘点只服务 Spike；最终 2022/6.3 对照必须使用冻结提交 C 的同一内容、同一运行状态。两个不同布局版本的差异不能直接判为引擎回归。
用户批准后若场景或业务内容再次变化，旧批准失效，重新验证批准并保留历史，不为了“只有一次批准”掩盖后续修改。
不将代码冻结延长至 F/E2，否则首个功能批次无法开始。H 前保持冻结，H 验收通过即解除。

### 7.1 内容先完成时的 CP

触发：内容已完成并通过 2022 归因验证，而 MIG 尚未具备最终汇合条件；不再要求内容先合入 main。
默认保持冻结。提交延误原因、热点文件和预计剩余工时，由用户选择继续冻结，或精确解冻 CONTENT 的文件/任务。
若选择提前功能开发：只在 content/p0p1-layout 的 2022 环境工作；避开迁移热点；MIG 在登记节点吸收新增内容；原 C 作废，最终重新冻结并记录 C2 的 2022 对照。main 不直接开发。
累计迁移达到 12 人日仍无法闭合时，暂停扩大修复范围并报告完成项、剩余风险和新的估计；保留现有成果，不自动删除或继续加预算。

## 8. 临时项解除登记和 E2 清理

每项临时变更登记：ID、目的、绝对路径/配置键、原值/哈希、实际改值、生效节点、责任侧、解除条件、恢复动作、验证证据、状态。未实际创建的项标“不适用”，不得声称已清理。
本批次用手动 MIG- 台账；不复制原控制台线程、heartbeat、自动任务编号或旧租约。

| 项目 | 解除/处理节点 | 处理要求 |
| --- | --- | --- |
| 内容代码冻结、临时文件独占、禁止迁移回流 | E1 后 H 接棒通过 | 登记解除；main 禁止直接开发和资源安全规范持续有效 |
| 场景保存限制 | M4 精确范围开放 | 仅对已冻结输入和审定清单，保留 diff/恢复记录 |
| 过渡宏、临时依赖路径、影响生产的诊断 | M4 最终回归之前 | 移除或收敛为正式实现并重验 |
| Tag/Layer、字形、导表挂起单 | E1 前 | 完成本批次必要项；未来业务项显式转交，不混为迁移完成项 |
| 项目外共享设置临时改动 | 不再需要即恢复，H 必须核对 | 只撤销本批次差异，不恢复整个旧快照覆盖用户新设置 |
| 工作区路由标识 | S 至 E2，涉及保留副本时继续明确目标 | E2 后撤销仅为迁移双线新增的要求 |
| MIG 的临时 MCP、队列/进程、工具配置 | H 后停用，E2 移除 | 默认不创建；若实际引入，按 PID、端口、绝对路径精确定位；不影响原有日审/流水线 |
| MIG 输出、输入和临时坑号 | E2 前归档/映射 | 原路径、哈希、版本、输入、测试日志、人工批准、实际操作记录全部可检索 |
| MIG clone、Hub 条目 | F 后 E2 清理 | 校验归档和推送，关闭该实例，移除对应 Hub 条目，验证解析路径再删除准确目录 |
| 2022.3 工程回退环境 | 保留到 F | E2 确认恢复包可用；不自动卸载可能被别的项目使用的系统级 Editor |
| 三条新增分支与两基线标签 | 独立于 E2 | 分支保留；后续按用户阶段管理指令处理，backup 与基线标签作为回退锚点 |

H 时登记首个功能批次的实际 ID、范围和验收条件。若当时尚未安排功能工作，登记明确的下次审查时间；建议 H 后 7 个自然日设人工检查点，仅写台账，不创建自动提醒。
检查点只能决定有期限的保留、安排功能批次，或用户认可的替代稳定性验收；到期不自动删除文件，也不伪报 E2。任何延长要写明新的检查点，不能写无限期“另行决定”。

E2 清理链路：
1. 汇总两侧输入输出、M0-M4、C、E1/H/F 证据，确认所有未完事项有承接目标；受保护探针、Lab、未来 Spine 预研不得当作迁移垃圾清理。
2. 在开发分支组织最终归档包，目标 D:/GameProject/Eat-What/.ai-workspace/archive/MIG63_<关闭日期>/；按项目既有 LFS 归档方式入库，生成 manifest、SHA-256 和恢复说明，核实可读取。
3. 若需要持久化的证据或提交尚未得到允许的推送，保留副本，不执行删除。归档/提交/推送沿用户确认的执行范围进行，不在 main 直接制作归档成果。
4. 验证 MIG 无未知改动、无独有未保存成果、无未推送提交；已忽略文件也必须核对，不能只看 git status 干净。
5. 停止确属 MIG 的 Editor/辅助进程，移除对应 Hub 条目、临时客户端连接和项目条目；不清理原工作区或共享工具。
6. 解析并核对 D:/GameProject/Eat-What-U6 的最终绝对路径，确认与登记的 MIG clone 一致，之后才删除该副本。
7. 检查原工作区仍可在固定 6.3 打开，分支/标签仍在远端、无悬空依赖路径、无迁移专用冻结/调度项；完成关闭记录。任何条件失败就保留未完成状态及具体原因。

中止链路：E1 前中止时保留原 CONTENT/2022；恢复本批次临时设置，归档原因和可复现问题，保留分支/预研。内容是否单独合入 main 按阶段验收决定，不自动合并。E1 后回退必须评估新增功能和资产，不把 2022 打开升级后目录当作回退方法。

## 9. 最终验收清单

- 固定 Unity 6000.3.x、包版本、目标平台和最终提交；全工程编译无新增阻断，Console 异常有结论。
- 现有离线回归通过；stubs 修改有具体 API 依据，测试未被削弱。
- C 对照的 ShortCycle 完整驱动、真实输入、滚动六层/五列、镜头、ESC/层级、数据和本地化保持约定行为。
- 同内容同状态的 TMP 字符、回退链、材质及动态实例检查通过，不硬套历史 155/216。
- VisualEffectsLab 中描边/阴影/合并/泡泡完成真实 GPU 和人工视觉验收，重复进退和资源释放正常。
- Spine两处预研功能保留，以用户指定Tomato 3.8.99完成上述Unity 6功能回归；旧Sample/output4的2022已存在数据故障单列，不等待无法溯源的重导出。不得只保存文件而宣布动画/换装/移动已迁移。
- Windows x64 独立 Player 从 ShortCycle 启动，通过约定流程；必要时补 Spine 诊断包。不得宣称未测试旧业务链已通过。
- Build/Profile 列表符合第 6 节；无正式构建误含测试场景。
- 无 URP_COMPATIBILITY_MODE；无指向临时 clone 的依赖；包和 LFS/忽略资源能恢复。
- 生成数据、资源 GUID、Missing Script/缺失引用、必要重序列化、根文档增量全部在最后回归前完成。
- 用户本人批准最终场景哈希及提交 SHA；合并/tag/远端实际引用与验收记录能对应。
- E1、H、F、E2 分别记录，任何一项未达成不能跨阶段宣称全部迁移完成。

## 10. 工期、文档影响与证据

暂估 6–10 人日为规划值，首轮导入后按 Spine 和渲染问题更新；不包含新长循环功能、额外美术制作和人工等待。Spine 保留回归是必要成本，不能通过移除模块压缩估算。
M0 已建立分支、恢复快照和独立迁移副本，并隔离迁移侧 MCP 依赖；M1版本/包/配置及两个API转换脚本已发生导入差异，M2完成获批URP/Build设置和四个验证脚本适配。业务场景/Prefab无磁盘差异；原2022工作区未接收迁移生产变更。目标、Spine预研边界、Build决策及最新机器/人工验收状态在内容分支更新下列入口，之后随实际迁移维护：
- D:/GameProject/Eat-What/项目整体阅读理解与推进基线_2026-08-14.md：决策、风险和节点；修正“尚未确定目标版本”，保留当前未迁移事实。
- D:/GameProject/Eat-What/CODEBASE_MAP.md：补充 Spine 两场景及控制器、实际变更后的 Build 和验证路径。
- D:/GameProject/Eat-What/AGENTS.md：实际接棒到 6.3 后更新准确版本；本轮不提前改版本。
- 视效若实际改动功能/参数，同步相应中文组件说明；本轮无组件说明内容变化。

保留 v1.1 和本轮反馈原文作为输入，二者中的“已拍板”不替代用户的实际授权。
v1.1/总档需停止照用的表述：完整 Core asmdef 假设、已存在 deny 假设、第二控制台直接复制、双端口天然隔离、生成 .cs 延后、content→main→migration、打标签后补待办、main 上继续开发、E2 自动删除分支。

资料：
- Unity 6.3 升级指南：https://docs.unity.cn/6000.3/Documentation/Manual/UpgradeGuideUnity63.html
- Spine 官方安装/版本匹配说明：https://us.esotericsoftware.com/spine-unity-installation
- Spine 官方下载与 URP 配套包：https://us.esotericsoftware.com/spine-unity-download
- Unity EditorPrefs 存储说明：https://docs.unity3d.com/ScriptReference/EditorPrefs.html
- Unity 6.3 命令行与同工程实例限制：https://docs.unity.com/en-us/engine/6000.3/manual/unity-editor/command-line-arguments/editor

本稿的初始核验数据保留为历史事实；当前执行结果、恢复证据及待处理节点统一写入执行台账，不据本稿早期状态推断动作已完成。
