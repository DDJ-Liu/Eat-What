# MIG63 M4 汇合与最终候选

2026-09-21。C 已由用户确认冻结，M4 按该内容推进。此报告是最终候选的执行记录，E1 最终 SHA/人工视觉批准、main 集成、H 接棒、F 与 E2 仍是后续节点。

## 1. 冻结、合入与分支

- C：`65604ab9daed6f5aa14d1347e7b50a56bd1fd999`，`content/p0p1-layout`。用户明确回复“以当前内容冻结，继续 M4”。原工作区继续保持 2022.3.62f2，未接收迁移资产。
- S：`ec3aa7c6f3d12d5eeb8ebbaa561d227b174107d0`。C 相对 S 没有新增布局、场景、配表或业务源码，只有文档/保护工具；4272 个内容文件形成冻结清单，M0 恢复包中的 840 个工程文件与当前内容逐字节一致。因此沿用同内容的 M0 2022 运行证据，**本轮没有重新运行 2022 Editor**。
- C → MIG 合入提交：`0eabe4a922d165a975150aef9ad2c7b7edb9a96b`。唯一冲突为 `.gitattributes` 同位置插入，保留 TMP LFS 和 hook LF 两组规则；没有手工拼接 Unity YAML。
- 仅 MIG 的本地 `mig63.phase` 切到 `integrating`。本轮场景保存限定为两处 Spine 参考接线及必要的 Unity 自动序列化；原 CONTENT 冻结保持。旧骨骼没有被覆盖，新美术导入仍归 CONTENT。
- main、backup 保持 S。最终候选的实际提交/推送 SHA 与远端核对另见本轮交付记录，不将 C 冻结当成 E1 批准。

## 2. MCP 依赖收敛

`LocalPackages/com.coplaydev.unity-mcp/` 保存上游 716 个文件，manifest 使用工程内相对路径。上游完整 SHA 为 `c21bf496bca87d54e75bad048563c3adb1782081`，Editor 版本 10.1.3-beta.4，同提交 Server 版本 10.1.2。记录 MIT 许可、服务端 pyproject/uv.lock，未制作完整服务端离线镜像。

`DevTools/MCP/README.md` 提供恢复与路由步骤；来源、上游文件清单和 10 个本地补丁文件的前后 SHA 均入库。局部 `.gitattributes` 禁止这些上游快照被行尾转换，保证新 clone 可按同一 SHA 核验。UPM 新增 Newtonsoft JSON 3.2.2 间接依赖，其余原包版本保持；Unity 自动加入的 multiplayer.center、visualscripting、模块包本批有意保留，后续精简另批执行。

本轮用固定源码、Python 3.11.9、`uv sync --frozen --no-dev` 恢复服务环境，localhost:8080 实测连接到 `Eat-What-U6@6eb3fd4c46b5b4ee`。每个新会话选择实例，Editor API 再校验绝对路径和 6000.3.24f1；未重启原自动队列，未给原 2022 工程挂接这个会话。

**配置副作用单列：**首次导入时，上游 StartupConfigRewrite 写入 Claude Desktop 配置，临时 backup 被上游代码删除，无法确知旧内容。当前条目为宽范围 prerelease 命令；用户明确选择“先保全并保持现状，本轮不再改客户端”。当前快照留本地证据，SHA=`7BCF4D48985744F2D99BFB88366537CC0AB16B78243660F3976E2D8D2C6DB06A`，后续重载核对未变。Codex 配置修改时间未变。仓库版已将默认状态检查改成只读，并取消 stdio 版本/旧 ServerSrc 自动迁移调度；主动 Configure 保留。不能把“已保全”写成“已还原”。个人客户端配置不入库。

## 3. 导表、资源与设置

| 检查 | 结果 |
| --- | --- |
| 两次 Import All | 首次创建0/更新16/未变105；第二次创建0/更新0/未变121。121个生成ScriptableObject的Editor语义JSON、GUID前后相同，Git无业务差异。16个Tag仅首次规范化写出；占位Sprite警告保持原有导表内容，不在此批替换美术 |
| 资源结构 | 六个相关场景、Assets/Prefabs与Assets/Resources下71个Prefab、121个生成SO没有Missing Script或非空失效对象引用。122个generated asset中的另一个是共享占位材质，不混计为SO |
| 字体 | ShortCycle包含未激活对象仍为232个TMP_Text，字体/共享材质路径已记录；字体和TMP目录没有最终持久差异。一次重载触发动态图集漂移，探针拒绝继续；保全完整after并经Editor整份恢复，批准SHA保持 `1D379A…399CC2`，未设置提交豁免 |
| Build/Profile | ShortCycle index0，CookingProcess index1，二者启用；platform-default无override。Prepare文件及两个Lab/两个Spine预研都保留，测试场景未进正式列表 |
| 渲染/输入 | Compatibility序列化值false，无URP_COMPATIBILITY_MODE；D3D11第一/D3D12第二、Auto=false。Uniform滚轮行为按用户签收保留；Mac适配/手感继续MIG-F01 |
| Tag/Layer与路径 | 保留原索引/名称，生成GUID及Resources契约未迁移重命名，无指向临时clone的最终UPM依赖 |

## 4. Tomato 正式参考接线

仅在已冻结内容汇合后的 MIG 中，经 Editor API 保存：

- `Assets/Scenes/Spine Sample.unity`：原两组对象改接现有 Tomato 3.8.99、Tomato_Non基础皮肤与7个换装部位；保留场景、对象/Prefab身份及按钮。Sample 的 `Bottom` 槽名称与 `Test_EquipBottom` 调用契约保持，不能统一改名后让原按钮失效。
- `Assets/Scenes/ToolTests/HorizontalPlayerControllerTest.unity`：保留原Rigidbody2D移动、边界、翻转和Animator引用链，改用同一Tomato；原Renderer延迟启用组件保留。
- `Assets/Scripts/Spine_Package/Avatars/Tomato/Tomato_MIG63.controller`：使用Tomato实际动画时长的idle/walk/kick clips；Idle/Walk/Kick触发器，kick后回idle，重复按钮触发可被消费。旧Sample/controller、Sample/output4骨骼/Prefab/GUID完全保留。

保存差异还含Unity6的RenderSettings/Lightmap/Light2D、MeshRenderer等正常序列化补全；不修改共享Prefab、业务源码、Spine runtime或Shader。

**MIG-P05：保存场景的旧Renderer恢复问题。**首次真实Spine Sample重复加载暴露Invalid worldAABB；原临时内存夹具未覆盖这一条。沿用现有 `SpineRuntimeMeshRendererBootstrap` 为Sample两个Renderer补延迟启用，磁盘禁用、Start后启用。修复后两场景连续两轮均无Error/Exception/Assert。登记为本轮参考接线/生命周期修复，不武断归因为Unity6新回归。

`Assets/Editor/MIG63Validation/MIG63SpineSceneProbe.cs` 长期保留，与既有滚轮/Tomato工具共享会话恢复。新增菜单验证保存场景、动画按钮入口、换装事件绑定和Renderer恢复，不替代OS真实鼠标点击与用户观感。探针调试中曾误用仅selectEvent而遗漏delayedSelectEvent，另有字体拦截导致启动超时；失败记录保留，修正后按新时间目录重新运行，不把这些工具失败计为产品通过。

## 5. 验证结果与限制

- 九组既有离线回归全部通过；没有削弱断言或重写生产渲染路径。
- 保留的滚轮探针与Tomato双组件GPU探针通过；Spine保存场景最终版各连续两次通过，覆盖两角色、保存引用、有效网格、材质、idle/walk/kick、左右移动/停止/翻转、换装及恢复。截图可见角色，未见错误粉色材质；不声称全部皮肤/混合模式/autoOptimize已验。
- ShortCycle完整业务探针、VisualEffectsLab、FridgeScrollLab及ShortCycle/VFX重复进退五组通过，本轮运行段无Error/Exception/Assert。ShortCycle为232 TMP；VFX泡泡8/16/清空、自有RT退出释放均正常。Lab Modal仍只是层比较，真实阻断沿用用户人工验收，未用该Lab结果冒充物理阻断。
- CookingProcess在Editor加载并Play，无该时段运行异常；只验可加载，不扩大为旧业务链验收。
- 最终Windows x64 Development构建Succeeded，0 errors、14 warnings，27.64秒、968981069字节。警告包括已知弃用API、未使用字段、Shared Set提示、两条2022已有的描边潜在未初始化警告。
- 正式成品按普通输入/图形模式隐藏窗口启动，D3D11，进入ShortCycle Cover，日志无Error/Exception；通过精确PID停止测试，**没有验证用户正常点击退出**。独立诊断包实测ShortCycle index0 → CookingProcess index1 → ShortCycle index0，三段errors=0，完成标记存在，进程自主退出码0。诊断脚本与meta已通过Editor移除，不进入最终Git成果；正式成品本身在添加诊断脚本之前构建。
- 首次成品冒烟误用了`-batchmode`，该模式没有Mouse.current，旧Tools.getMousePos直接读取导致空引用循环。该次作为失败记录保留；已停止对应进程并改用普通窗口模式复验。没有为不属于发行目标的无输入批处理模式修改游戏代码，也不把那次日志写成通过。

M4业务/VFX机器截图为当前GameView的635×558，不能用于宣称已完成2022/6.3同分辨率逐像素对照。Tomato专项相机图为1920×1080。用户之前已签收视觉与滚轮；本轮新增Spine保存接线及最终候选仍需人工检查。

## 6. 人工验收与下一步

1. 在MIG 6000.3.24f1打开两处Spine场景，查看Tomato显示；Sample试Idle/Walk、头发A/B、下装、素体；Horizontal试A/D或左右键、停下与转向，连续进退Play两次。
2. 用本轮 `Builds/MIG63/Windows-x64-M4/EatWhat.exe` 做最终可见窗口短流程与正常退出；复查慢速滚轮、快速后停手、反向、移出区域/切窗、正式弹层阻断。滚轮功能已完成，此处为整合版回归，手感微调不重开MIG-P04。
3. 最终视觉对照在两个Editor使用同一个固定分辨率，重点描边边缘/Alpha、TMP SDF与整体色调。原2022仍用冻结C内容，勿用2022打开迁移副本。
4. 检查交付中的最终提交SHA和场景哈希后批准E1；再按既定链路集成main、打基线标签、H原工作区接棒。H通过才解除代码冻结。F/E2、分支保留与副本清理继续按台账，不提前删除。

MIG-F01（统一单位、Mac与手感）以及CompanyName/ProductName在存档系统落地前处理、官方CLI/MCP/额外模型评估留后续批次。没有因此启动新业务开发。

## 7. 证据和文档

原工作区稳定证据：`.ai-workspace/outputs/control/MIG63_执行_20260921/M4/`；含C清单、333文件写前快照、API代码/结果、源文件补丁来源、运行/失败历史、构建日志和最终哈希。迁移侧本轮输出归并到该目录，再做E盘独立备份；这些本地证据在E2统一纳入正式归档。

根项目基线、CODEBASE_MAP、AGENTS、执行台账、临时项、DevTools/MIG63和DevTools/MCP同步更新。原工作区文档保持C冻结版本直至H接棒；本轮最新入口在迁移副本。没有改变滚轮参数或视效组件配置，本轮无新的视觉特效中文说明变更；Spine测试与使用入口写入DevTools/MIG63。
