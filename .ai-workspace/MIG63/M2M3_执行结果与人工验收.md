# MIG63 M2/M3：执行结果与人工验收

> 后续状态（2026-09-21）：本文为 M2/M3 执行时点报告。用户已完成初验和 Windows 人工跑测，除滚轮外无明显问题；两次快照提交已推送，Spine 改用 Tomato，TMP 已按 N1 入库。最新处理、风险与验收节点见[M3R 人工反馈处理与补漏](M3R_人工反馈处理与补漏.md)。下文“未提交”“等待旧骨骼重导出”等不再代表当前行动项。

实际核验：2026-09-21。依据用户“按清单推进”执行 M1 清单。结论：URP 构建阻断已解除，Unity 6.3 的既有离线回归、三类场景机器回归和 Windows 启动冒烟检查通过，可以继续迁移；人工视觉/真实输入、Spine 重导出及最终集成尚未完成。

## 1. 分支、版本和操作边界

- 原工作区 `D:/GameProject/Eat-What`：`content/p0p1-layout`，2022.3.62f2；本轮只维护迁移及项目入口文档。
- 迁移工作区 `D:/GameProject/Eat-What-U6`：`migration/unity-6.3`，6000.3.24f1 / 4e7b9b5b6244。
- 收尾实时远端查询：content 为 `88404e29ab20b4e5abb5462006516981cc0f13ac`，migration 为 `40a4d3f244e99d28e7b45e05ed7213165cbb10f9`；main 和 backup/pre-unity6-20260920 均仍为 `ec3aa7c6f3d12d5eeb8ebbaa561d227b174107d0`。三条新增分支保持对应 origin upstream。
- 本轮未提交、推送、合并或打最终标签；本轮成果仍在两个工作区的未提交差异中。远端分支存在不表示首次导入/M2成果已上传。
- 没有改生产业务逻辑、Shader、业务场景或 Prefab；两个业务脚本的 `linearVelocity` 变化沿用已留档的 M1 API Updater 结果。Spine 代码、骨骼和预研场景均保留。

## 2. 已执行的配置和代码变化

| 路径（相对迁移工作区） | 实际变化 |
| --- | --- |
| Assets/UniversalRenderPipelineGlobalSettings.asset | 经 Editor SerializedObject API 核对并保存 Compatibility=false；未添加 URP_COMPATIBILITY_MODE。执行前内存已是 false 且 dirty、磁盘仍为 true，已经保留内存 JSON；不推断是谁先点了官方切换按钮 |
| Assets/DefaultVolumeProfile.asset（补正） | M1 空 Profile 在 M2 默认初始化中扩展为 Volume 组件；GlobalSettings 同时补入引用。已包含在 M2 收尾151文件快照及108b092提交，前版报告遗漏了对此差异的解释；最终同分辨率色调对照仍保留 |
| ProjectSettings/EditorBuildSettings.asset | 经 EditorBuildSettings API 设 ShortCycle_P0P1 为启用 index 0、CookingProcess 为启用 index 1；移出 Prepare，保留文件。当前 platform-default Profile 不覆盖该列表 |
| DevTools/Rendering/Test-OutlineMerge.ps1 | 按 ProjectVersion 找 Hub/旧安装路径，支持显式 UnityEditorPath 并校验实际版本 |
| DevTools/Rendering/Test-EtherBubbleDistortion.ps1 | 同上；补 URP 17 的 Unity.RenderPipelines.Universal.2D.Runtime.dll 引用，仍兼容存在旧 2D.Internal.dll 的环境 |
| DevTools/ShortCycle/Test-CK01CShortCycle.ps1 | 移除固定 2022 安装路径，改为项目版本对应 Editor |
| DevTools/ShortCycle/CoordinateSpaceRegression/Invoke-CoordinateCompilation.ps1 | 按项目版本选择 Hub/旧安装路径并校验版本 |
| Assets/Resources/PerformanceTestRunInfo.json、PerformanceTestRunSettings.json 及 meta | 先保留机器信息产物，再经 AssetDatabase 删除；没有删测试依赖 |

四个测试脚本仅调整环境定位/程序集引用，原断言保留。它们是应随迁移保留的工具适配，不是本轮结束要删除的临时工具。

构建/刷新期间另外观察到三项设置序列化补全：ProjectSettings.asset 写出 Standalone static batching=1/dynamic batching=0；SceneTemplateSettings.json 增加 UnityEngine.PhysicsMaterial 类型；ShaderGraphSettings.asset 增加 overrideShaderVariantLimit=0、customHeatmapValues=null。均已逐项保存前后证据；保留 Unity 当前结果，不手改 YAML。M1 已有 Graphics API 等变化仍保留，来源未确认的内容不归为“本轮人工调整”。

## 3. 验证结果和实际覆盖

| 检查 | 结果及限制 |
| --- | --- |
| 九组既有离线回归 | 全通过：ShortCycle、描边合并、阴影、泡泡、导表契约、schema、CSV、坐标、标签格式。Outline 95 runtime + 88 static；Bubble 68 + 52；Shadow 78 + 50；ShortCycle 编译92源码、216静态绑定断言。部分使用 stubs，不等于GPU或硬件输入验收 |
| ShortCycle（两轮） | 现有探针通过 cover/catalog/select/browse、start/return、镜头往返、输入锁、线索锚点、P1保持、分层ESC清空及生成菜谱链；含未激活对象 TMP_Text=232，与M0口径一致 |
| VisualEffectsLab（两轮） | 现有驱动器临时激活；停自动发射、清池后发8个，RT捕获有效；开/关实际截图；追加发射到16上限；清池后0。描边活跃样例共12张自有RT，LastRenderedFrame持续更新、LastError为空；没有重写渲染路径 |
| FridgeScrollLab | SIMULATED 六层往返、首尾边界及 Modal 栈顶切换通过。Lab 的两个输入层原本都配置 Everything，Modal 是层比较，不据此宣称物理阻断通过；直接 Dispatch 本来也绕过物理命中筛选 |
| 重复进入/退出 | 五个用例完成；退出后的实际 RenderTexture 清单未发现本轮组件命名对应的 Mask/Candidates/EtherBubbleSceneColor 自有RT残留。不是全工程内存泄漏证明 |
| 最终运行错误 | 最后一轮 play/ 无 Error/Exception/Assert 记录；更早的 Spine 和临时测试工具错误保留，不清空 Console 冒充修复 |
| Windows x64 Development Build | Succeeded，0 errors / 14 warnings，65.55秒，966,050,051字节；URP前置检查不再阻断 |
| Windows Player 启动冒烟 | 实际进程加载6000.3.24f1、D3D11、Input System，初始化到 ShortCycle Phase0/Cover；日志未检出异常、LogError/LogException或Shader error。该次为隐藏窗口启动，随后受控停止，不算正常退出/完整交互验证 |
| 清理后编译 | 两个临时 Editor 工具及meta均已通过AssetDatabase移除，重编译Tundra success；剩既有Editor弃用警告 |

本轮最终截图为785×587，M0为2560×1440，不能做同分辨率逐像素一致性结论。HDR/Alpha、遮挡、描边宽度/阴影模糊组合、字体回退/换行及真实鼠标手感仍需人工完整复核。现有GPU证据说明已观察到渲染、开关变化和资源释放，不能替代全部视觉验收。

## 4. 仍需处理的风险

1. **MIG-P01，E1阻断：Spine旧数据。** Sample/output4仍3.8.75，须正规3.8.99重导出；尚未收到新文件。不能删除预研、改文件头或屏蔽异常。
2. **MIG-P03，待视觉复核：2条描边Shader编译警告。** `Assets/Shaders/2D/SpriteOutline2D.shader:133/156`，D3D11报告 FindNeighbourAlpha / SampleShadowAlpha potentially uninitialized。已读源码但未证明是误报，也未复现确定画面故障；在Lab复核厚度分支、硬/软阴影、HDR/Alpha后再决定是否做最小修复。
3. 其余12条Build警告：4条弃用API、6条未使用字段，以及2条ClueBoard_Group共享Set允许合并的项目校验提示。本轮不扩大为全面弃用API重构。
4. 物理输入和成品窗口画面未完成：Computer Use截图接口在本设备报不支持；可见Player启动请求又发生工具授权等待超时。键盘操作Editor和Unity内截图可用，因此机器检查已完成，但不能声称已操作过真实鼠标链。
5. MCP仍隔离。正式AI连接方式须在M4前落实可恢复依赖并验证；本轮没有安装新模型、连接器或创建第二自动队列。

## 5. 恢复、错误史及证据

原工作区输出：`D:/GameProject/Eat-What/.ai-workspace/outputs/control/MIG63_执行_20260921/M2/`。

- `final-check-summary.json`：156项保护文件核对，Assets变化0；4项ProjectSettings差异为上述Build列表及三项序列化补全。独立核对忽略TMP84文件，与M1相比0变化。
- `final-files/`及`final-files-manifest.json`：收尾151文件、5,965,441字节，副本逐文件SHA核对；包含首次导入后的变更和TMP，不包括约966MB的可再生成Player二进制。
- `editor-M2/`：设置内存快照、构建报告/警告、Play JSON/PNG、清理记录；`editor-M3/`：Player日志及受控停止记录。另有四个工具适配diff、9组离线日志、脱敏Editor日志。
- `editor-M2/play/`是最终一轮有效机器/截图证据，5项PASS；`play-attempt1..5/`保留工具调试历史。失败来源依次涉及未激活父对象、自动发射影响计数、暂停时禁止发射、错误地假设Lab业务Dispatch受Modal物理筛选。第5轮机器通过但截图紧接状态切换导致帧末错位，最终轮已把截图和后续状态变更分开。
- 初始未命名场景含一个根对象且clean，测试前通过SaveScene(saveAsCopy)留在 `editor-M2/play-attempt1/initial-untitled.unity`（及meta，Assets外）；收尾回到已保存的ShortCycle、Edit/clean。没有保存生产场景、字体或用MarkSceneClean掩盖变化。
- 临时工具源及meta保留在证据内；Assets中的两工具/meta和两个Performance JSON/meta共8路径均不存在。Player冒烟进程已结束；交互Editor保留供人工验收。
- Git差异复查：业务场景/Prefab及Assets/Font无差异。迁移侧完整`git diff --check`仍提示Unity序列化新增空字段的行尾空格（URP资产、旧Aseprite meta和ProjectSettings）；不手改YAML做格式清理。四个测试脚本和原工作区文档的专项diff检查通过。

## 6. 下一步按此顺序进行

1. **人工初验：只使用迁移副本6000.3.24f1。** 打开 `Assets/Scenes/Cooking/ShortCycle_P0P1.unity`：封面→目录→选菜→P1→返回、滚轮正反向/边界、弹层阻断与ESC、字体/行距/裁切。退出Play后不保存测试激活状态。
2. **统一Lab视觉验收。** `Assets/Scenes/ToolTests/VisualEffectsLab.unity` 按既有中文组件说明检查描边/阴影的厚度与模糊组合、遮挡、HDR/Alpha、泡泡开关。`FridgeScrollLab.unity`检查真实鼠标命中、方向、焦点与手感；Lab的Modal层比较不替代正式场景弹层验收。既有Debug驱动器仅在Play临时启用。
3. **Windows成品操作。** 双击 `D:/GameProject/Eat-What-U6/Builds/MIG63/Windows-x64/EatWhat.exe`，复核启动即ShortCycle及上述主要输入/字体/画面，再正常关闭。默认后续日志在 `%USERPROFILE%/AppData/LocalLow/DefaultCompany/Button Project/Player.log`；本轮固定日志副本另存M3。反馈异常时注明场景、操作、现象，不把程序生成成功当作本项完成。
4. **准备Spine正规导出。** Sample/output4各自提供3.8.99的骨骼、atlas和匹配贴图，保留源.spine；先放Assets外并给出路径。随后逐文件出哈希/最终路径/旧引用/动画名/slot/attachment替换表，经资产审查后导入，保留SkeletonData/材质/Prefab等既有GUID，再验Spine Sample及HorizontalPlayerControllerTest的idle/walk/转向/换装/重复Play。
5. **闭合M2/M3后进入C/M4。** 核定内容冻结SHA C、确定AI依赖、保存恢复点，再汇入内容侧变化并在Unity 6处理最终序列化、字体/导表/构建回归。根入口文档由CONTENT维护，MIG目前保留本轮增量输出，不提前覆盖两侧归属。
6. **最后执行E1→H→F→E2。** 用户按精确SHA和视觉结果验收后，才集成main/最终标签；原工作区Unity 6接棒通过后解除冻结；首个功能批次验收、证据正式归档校验后再清理副本。三条分支的去留独立决定。

文档影响：已更新执行台账、基线及两个项目入口；本轮没有变更视觉组件功能、默认参数或配置入口，因此无需改组件使用说明。M2/M3整体仍待人工初验与Spine闭合，不进入E1。
