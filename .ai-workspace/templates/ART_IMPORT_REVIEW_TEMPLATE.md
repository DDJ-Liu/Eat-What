# 美术资产工程导入审阅模板

状态：待审核；此文档生成时尚未向Assets写入本批资产。填写实际核对日期、同步/日报来源及版本。用户已直接给定的明确选择标“已授权”，不再次询问。

| 源相对路径/内容说明/SHA | 工程现有对应路径/GUID/同图或同名情况 | 处置（新增/替换/复用/迁移/暂缓） | 最终完整路径与文件名 | 改名原因/用途/预期场景对象 | Importer与加载契约 | 引用影响/精确备份与回退 |
|---|---|---|---|---|---|---|
| 逐文件填写，不以整个中转文件夹替代 | 无则明确“无” | 未来素材明确是否仅同步或正式目录未绑定 | 暂缓项目填“不导入，保留同步区路径” | 对照图可单列，仅参考不自动部署 | PPU/Pivot/Mesh等与已存在工具搜索目录 | 替换、删除、共享资源变化单独列清 |

## 配置草案

逐项列Scene/Prefab/生成数据完整路径、对象/字段、现值、目标、保留的交互/状态机/本地化合同、验收和回退方式。未知状态逻辑不能当图片替换悄悄实现。列出本次执行终点与后续待定内容。

## 审阅与执行记录

保存真实用户决定及覆盖范围；执行前复核SHA与审定版一致。审批后直接进最终目录，同图复用、迁移保GUID。除用户明确要求外不建立ArtImports等中转目录；若有批准的临时目录，列完整清理对账。执行后追加最终映射/引用/验证和待人工项目，不用计划值代替实测。
