# 食材交互与反馈系统分析报告

## 执行摘要

本报告对ButtonProject中的食材交互与反馈系统进行深度分析，目标是理解食材的表现能力和反馈机制，为Ingredient_Interaction的设计提供基础。

## 架构概览

### 核心交互流程
```
食材 (Ingredient_Cooking)
  ↓ (放入容器)
容器/DropZone (CookingDropZone)
  ↓ (内容变化触发规则匹配)
管理器 (CookingManager)
  ↓ (选择合适工具)
工具 (Tool_Cooking/Stir/CutDice)
  ↓ (交互完成)
食材反应 (Ingredient_Interaction.playReaction)
  ↓
动画播放 (Animator.Play)
```

## 关键文件分析

### 1. Ingredient_Interaction.cs
**位置**: Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs

核心功能:
- 管理食材的动画反应
- Animator组件获取: `anim = GetComponent<Animator>()`
- 动画播放: `anim.Play(animName)` (带参数验证)
- 交互完成回调: `reactionDone(int channel)` (当前为空)

关键方法:
```csharp
public void playReaction(string animName)
{
    if(Tools.AnimatorHasParameter(anim, animName))
    {
        anim.Play(animName);
    }
}
```

### 2. InteractionController.cs
**位置**: Assets/Scripts/Cooking/Interaction/InteractionController.cs

基础控制器，提供:
- 容器、工具、食材的引用管理
- 资源路径配置 (prefab加载路径)
- Initialize(container, tool, ingredients) 初始化方法

### 3. CutDice.cs (多阶段交互示例)
**位置**: Assets/Scripts/Cooking/Tool/CutDice.cs

多阶段交互模式:
- 两个ScrollBar (slice1, slice2)
- 阈值触发: triggerThreshold (0.3), doneThreshold (0.95)
- 自动驱动: AutoDriveToOne() 协程
- 阶段转换: onSlice1Done() → onSLice2Done()

关键流程:
```
slice1被激活 → 用户拖拽达到threshold → 自动驱动到1.0
→ onSlice1Done() 切换到slice2 → 重复 → onSLice2Done()
→ onInteractionFinished()
```

### 4. Stir.cs (连续反应示例)
**位置**: Assets/Scripts/Cooking/Tool/Stir.cs

圆形运动检测:
- 位置采样: `_positionSamples` (大小: sampleBufferSize)
- 距离积累: `_accumulatedDistance` (触发阈值: minTriggerDistance)
- 圆形判定: `IsCircularMotion()` (绕数算法)
- 外部完成: `CompleteInteraction()` (供动画事件调用)

关键特性:
- 实时采样当前拖拽位置
- 绕数算法: `|netSignedAngle| / cumulativeAbsAngle >= windingRatioThreshold`
- 触发回调: onInteractionTriggered (当前为空)
- 完成回调: onInteractionFinished (当前为空)

### 5. Ingredient_Cooking.cs
**位置**: Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs

食材管理:
- 继承 SpawnedPlaceable<Ingredient_Cooking>
- 放置回调: onPlaceRight(), onPlaceCancel()
- 拾取回调: onPickUp()
- DropZone追踪: 记录当前所在容器

关键交互点:
```
onPlaceRight() → CookingManager.onItemReleased()
onPickUp() → CookingManager.onIngredientInHand()
```

### 6. CookingDropZone.cs
**位置**: Assets/Scripts/Cooking/CookingDropZone.cs

容器内容管理:
- contents: List<Ingredient_Cooking>
- TryAcceptIngredient(): 验证食材是否符合规则
- onDrop(): 处理拖放事件
- NotifyContentsChanged(): 触发规则匹配

内容操作:
```csharp
RemoveContent() → 移除食材
RestoreContent() → 恢复食材
ClearContents() → 清空所有
```

### 7. CookingManager.cs
**位置**: Assets/Scripts/Cooking/CookingManager.cs

核心协调:
- FSM状态管理: ingredientChooseState, ingredientPlaceState, InteractionState
- 规则匹配: onDropZoneContentsChanged()
- 交互完成: onInteractionDone(bool isFinal)

交互完成流程:
```
onInteractionDone(true)
  ↓
消耗zone.contents
  ↓
实例化rule.output (新食材)
  ↓
清空DropZone
  ↓
返回ingredientChooseState
```

### 8. Tool_Cooking.cs
**位置**: Assets/Scripts/Cooking/Tool/Tool_Cooking.cs

工具基类:
- onAcceptedByZone(zone): 工具被放入容器时
- onInteractionDone(): 交互完成回调
- OnPlaceCancel(): 取消放置时恢复位置

### 9. CookingPhaseState.cs
**位置**: Assets/Scripts/Cooking/CookingPhaseState.cs

InteractionState类:
- 存储上下文: contextRule, contextZone, contextTool
- Setup(): 状态进入前的注入点
- EnterState(): 日志记录和初始化

## 反馈机制分析

### 回调链路
```
Tool.OnInteractionDone()
  ↓
CookingManager.onInteractionDone()
  ↓
rule.output实例化
  ↓
Ingredient_Interaction.playReaction()
  ↓
Animator.Play(animName)
```

### 事件触发模式

1. **直接方法调用**: Tool → Manager → Ingredient
   - 简单且低延迟
   - 用于立即反馈

2. **协程延迟**: CutDice.AutoDriveToOne()
   - 自动进度推进
   - 带用户拖拽中断检测

3. **动画事件**: Animator → reactionDone()
   - 精确的动画帧对齐
   - 用于多阶段动画同步

### 多阶段动画模式

**模式1: ScrollBar驱动 (CutDice)**
- 阈值触发 → 自动驱动 → 完成回调
- 优点: 用户可中断, 自动完成
- 用途: 切割、调味等多步骤操作

**模式2: 外部完成 (Stir)**
- 距离/圆形检测 → 外部调用 → 完成回调
- 优点: 灵活的触发条件
- 用途: 搅拌、混合等连续操作

## 设计建议

### 1. Ingredient_Interaction扩展点
- reactionDone(int channel) 应实现状态管理
- 支持多个并发反应
- 集成粒子效果、声音反馈

### 2. 动画控制标准化
- 统一参数命名约定
- 支持动画队列 (多个animName顺序执行)
- 超时保护机制

### 3. 反馈完整性
- 视觉: Animator动画
- 声音: AudioSource播放
- 物理: Rigidbody状态变化
- UI: 进度条/效果指示

## 总结

ButtonProject的食材交互反馈系统采用事件驱动的FSM架构，通过直接方法调用、协程和动画事件组合实现多层次的交互反馈。Ingredient_Interaction作为食材动画表现层，已建立了基础框架，可通过扩展reactionDone()和添加多个Animator参数来支持更复杂的反馈场景。


## 关键发现

### 1. 架构概览
- **FSM 模式**：整个系统基于有限状态机（FSM）架构
- **事件驱动**：通过回调和事件系统协调组件间通信
- **分层设计**：CookingManager 作为核心协调者，Tool 和 Ingredient 作为交互执行者

### 2. 核心交互流程
```
Ingredient_Cooking (放置)
  ↓
CookingDropZone (接收)
  ↓
CookingManager.onDropZoneContentsChanged() (规则匹配)
  ↓
Tool.onAcceptedByZone() (工具激活)
  ↓
InteractionState (交互进行)
  ↓
Tool.onInteractionDone() (交互完成)
  ↓
CookingManager.onInteractionDone() (消耗食材，生成结果)
```

### 3. 反馈机制
- **动画反馈**：Ingredient_Interaction.playReaction(animName)
- **事件回调**：onInteractionDone, onDropZoneContentsChanged, onIngredientInHand
- **进度跟踪**：ScrollBar 阈值（CutDice）、距离累计（Stir）、圆形运动检测

## 详细分析

### Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs
**路径**: `Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs`

**核心职责**：管理食材的动画响应和反馈

```csharp
public class Ingredient_Interaction : MonoBehaviour
{
    public Animator anim;
    
    public void playReaction(string animName)
    {
        if(Tools.AnimatorHasParameter(anim, animName))
        {
            anim.Play(animName);
        }
    }
    
    public void reactionDone(int channel)
    {
        // 反馈完成回调
    }
}
```

**关键方法**：
- `playReaction(string animName)`: 播放参数验证后的动画
- `reactionDone(int channel)`: 动画完成时调用，用于阶段转换

### Assets/Scripts/Cooking/Tool/CutDice.cs
**路径**: `Assets/Scripts/Cooking/Tool/CutDice.cs`

**核心职责**：多阶段切割交互（两次横纵切割）

```csharp
public class CutDice : InteractionController
{
    private bool _slice1Triggered, _slice1Done;
    private bool _slice2Triggered, _slice2Done;
    
    private void OnSlice1ValueChanged(float v)
    {
        if (!_slice1Triggered && v >= triggerThreshold)
        {
            _slice1Triggered = true;
            StartCoroutine(AutoDriveToOne(slice1));
        }
        if (!_slice1Done && v >= doneThreshold)
        {
            _slice1Done = true;
            onSlice1Done();
        }
    }
}
```

**交互模式**：
1. 第一阶段：slider1 达到 triggerThreshold 触发，达到 doneThreshold 完成
2. 第二阶段：自动推进后，slider2 重复相同流程
3. 自动驱动：未完成的阶段会自动驱动至 1.0

### Assets/Scripts/Cooking/Tool/Stir.cs
**路径**: `Assets/Scripts/Cooking/Tool/Stir.cs`

**核心职责**：检测圆形搅拌动作

```csharp
public class Stir : InteractionController
{
    private float _accumulatedDistance;
    private List<Vector3> _positionSamples;
    
    public void OnDragPositionChanged(Vector3 delta)
    {
        _accumulatedDistance += delta.magnitude;
        if (!_triggered && _accumulatedDistance >= minTriggerDistance)
        {
            _triggered = true;
        }
    }
    
    public bool IsCircularMotion()
    {
        // 基于绕数算法判断圆形运动
        float ratio = Mathf.Abs(netSignedAngle) / cumulativeAbsAngle;
        return ratio >= windingRatioThreshold;
    }
}
```

**交互模式**：
1. 累计拖拽距离触发（minTriggerDistance）
2. 采样位置点（每 sampleInterval 秒）
3. 计算圆形度数：净旋转角 / 累计转角 >= 阈值
4. 外部调用 CompleteInteraction() 标记完成

### Assets/Scripts/Cooking/CookingManager.cs
**路径**: `Assets/Scripts/Cooking/CookingManager.cs`

**核心职责**：全局状态协调，规则匹配与结果生成

**关键方法**：

```csharp
public void onDropZoneContentsChanged(CookingDropZone zone)
{
    // 触发规则匹配，检查食材组合
    // 调用 CommonMaterialPopup 显示结果
}

public void onToolPlacedIn(Tool_Cooking tool, CookingDropZone zone)
{
    // 工具被放入容器
    // 解析规则并进入 InteractionState
}

public void onInteractionDone(bool isFinal)
{
    // 交互完成，消耗食材
    // 实例化 rule.output
    // 清空容器，返回 ingredientChooseState
}
```

**FSM 状态**：
- ingredientChooseState: 选择食材
- InteractionState: 进行交互（存储 contextRule, contextZone, contextTool）

### Assets/Scripts/Cooking/CookingDropZone.cs
**路径**: `Assets/Scripts/Cooking/CookingDropZone.cs`

**核心职责**：容器管理，食材接收与验证

```csharp
public bool TryAcceptIngredient(Ingredient_Cooking ingredient)
{
    // 验证食材标签是否符合食谱规则
    // 检查数量限制
    return isValid;
}

public void onDrop(Draggable draggable)
{
    // 区分 Tool 和 Ingredient
    // 调用 CookingManager 的相应方法
    NotifyContentsChanged();
}
```

**内容管理**：
- contents: 当前容器中的食材列表
- RemoveContent(), ClearContents(), RestoreContent()

### Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs
**路径**: `Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs`

**核心职责**：食材生命周期管理

```csharp
public class Ingredient_Cooking : SpawnedPlaceable<Ingredient_Cooking>
{
    public void onPlaceRight()
    {
        // 放入容器
        CookingManager.onItemReleased(this, myZone);
    }
    
    public void onPickUp()
    {
        // 拿起食材
        CookingManager.onIngredientInHand(ingredientData, this);
    }
    
    public void onPlaceCancel()
    {
        // 取消放置
    }
}
```

**食材流程**：
1. 实例化 → idleState
2. 拖拽 → placingState
3. 放入容器 → onPlaceRight()
4. 进入 DropZone.contents

## 反馈与动画控制模式

### 多阶段动画模式
**CutDice 模式**（ScrollBar 阈值驱动）：
1. 每阶段有 triggerThreshold（触发）和 doneThreshold（完成）
2. 完成后调用 onSlice1Done() / onSLice2Done()
3. 自动驱动未完成的阶段（AutoDriveToOne 协程）

**Stir 模式**（距离 + 圆形检测）：
1. 累计距离达到 minTriggerDistance 触发
2. 采样位置点进行圆形检测
3. 外部调用 CompleteInteraction() 标记完成

### 反馈调用链
```
Tool.onInteractionDone()
  ↓
CookingManager.onInteractionDone()
  ↓
rule.output 实例化
  ↓
Ingredient_Interaction.playReaction(animName)
  ↓
Ingredient_Interaction.reactionDone(channel)
```

## 建议与设计指导

### 对 Ingredient_Interaction 的扩展建议

1. **完整化 reactionDone() 方法**
   - 当前为空实现，应添加动画完成后的回调逻辑
   - 可用于触发后续反应、销毁对象或通知管理器

2. **支持多参数动画**
   - 扩展 playReaction() 支持 int、float、bool 参数
   - 使用 Animator.SetInteger(), SetFloat(), SetBool()

3. **反馈事件系统**
   - 添加 OnReactionStart、OnReactionEnd 事件
   - 允许其他系统监听食材反馈

4. **动画阶段管理**
   - 实现 QueueReaction() 支持连续动画
   - 管理多个动画间的过渡

5. **性能优化**
   - 缓存 Animator 参数 hash（AnimationHashing）
   - 实现对象池重用

## 关键文件清单

| 文件路径 | 职责 | 关键方法 |
|---------|------|--------|
| Ingredient_Interaction.cs | 动画响应 | playReaction(), reactionDone() |
| InteractionController.cs | 交互基类 | Initialize() |
| CutDice.cs | 切割交互 | OnSlice1ValueChanged(), onSlice1Done() |
| Stir.cs | 搅拌交互 | OnDragPositionChanged(), IsCircularMotion() |
| Ingredient_Cooking.cs | 食材生命周期 | onPlaceRight(), onPickUp() |
| CookingManager.cs | 全局协调 | onInteractionDone(), onDropZoneContentsChanged() |
| CookingDropZone.cs | 容器管理 | TryAcceptIngredient(), onDrop() |
| Tool_Cooking.cs | 工具基类 | onAcceptedByZone(), onInteractionDone() |

---
**报告生成时间**: 2026/06/21
**目标**: 为 Ingredient_Interaction 设计提供基础


## 详细架构分析

### 2. 交互流程链

**流程链路：**
食材 (Ingredient_Cooking) → 容器 (CookingDropZone) → 管理器 (CookingManager) → 工具 (InteractionController) → 反馈 (Ingredient_Interaction)

```
用户拖拽食材
  ↓
Ingredient_Cooking.onPlaceRight(zone)
  ↓
CookingDropZone.onDrop() → TryAcceptIngredient()
  ↓
CookingDropZone.NotifyContentsChanged()
  ↓
CookingManager.onDropZoneContentsChanged(zone)
  ↓
ToolTagResolver.Resolve(tool, recipe) 
  ↓
CookingManager.InteractionState.Setup(rule, zone, tool)
  ↓
Tool.onInteractionDone() [CutDice / Stir]
  ↓
CookingManager.onInteractionDone(isFinal)
  ↓
Ingredient_Interaction.playReaction(animName)
  ↓
Animator播放动画
```

### 3. 关键文件详解

**文件路径：** Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs
```csharp
public class Ingredient_Interaction : MonoBehaviour
{
    InteractionController myController;
    public Animator anim;

    public void playReaction(string animName)
    {
        if(Tools.AnimatorHasParameter(anim, animName))
        {
            anim.Play(animName);
        }
    }

    public void reactionDone(int channel)
    {
        // 动画完成回调 - 待实现
    }
}
```

**文件路径：** Assets/Scripts/Cooking/Interaction/InteractionController.cs
```csharp
public class InteractionController : MonoBehaviour
{
    public ContainerTagType usedContainer;
    public ToolData usedTool;
    public List<IngredientData> usedIngredients;

    public void Initialize(ContainerTagType container, ToolData tool, List<IngredientData> contentIngredients)
    {
        usedContainer = container;
        usedTool = tool;
        usedIngredients = contentIngredients;
    }
}
```

**文件路径：** Assets/Scripts/Cooking/Tool/Stir.cs
- 继承自 InteractionController
- 关键方法：OnDragPositionChanged(Vector3 delta) - 累计距离追踪
- CompleteInteraction() - 外部完成标记
- IsCircularMotion() - 圆形运动检测

**文件路径：** Assets/Scripts/Cooking/Tool/CutDice.cs
- 继承自 InteractionController
- 多阶段滚动条控制：slice1 → slice2
- 自动驱动协程：AutoDriveToOne()
- 触发阈值：triggerThreshold = 0.3f, doneThreshold = 0.95f

### 4. 反馈机制模式

#### 事件驱动反馈链
```
onInteractionTriggered()
  → rule.ResolveCooking()
  → 规则匹配和验证
  → onInteractionFinished()
  → CookingManager.onInteractionDone(isFinal)
  → rule.output 实例化
  → Ingredient_Interaction.playReaction()
```

#### 多阶段动画控制

**CutDice 模式：**
1. slice1 滑块 - 触发阈值检测 (0.3f)
2. 自动驱动到完成 (0.95f)
3. onSlice1Done() - 切换到 slice2
4. slice2 滑块 - 相同流程
5. onSLice2Done() - 调用 onInteractionFinished()

**Stir 模式：**
1. OnDragStart() - 初始化采样缓冲区
2. OnDragPositionChanged() - 累计距离
3. 距离阈值触发 (minTriggerDistance = 5f)
4. IsCircularMotion() - 环绕数算法验证
5. CompleteInteraction() - 外部完成标记（动画事件或计时器）

### 5. 动画播放机制

**Animator 参数验证：**
```csharp
// 使用 Tools.AnimatorHasParameter() 检查参数是否存在
if(Tools.AnimatorHasParameter(anim, animName))
{
    anim.Play(animName);
}
```

**支持的动画类型：**
- idle - 空闲状态
- chop - 切割动画
- stir - 搅拌动画
- pour - 倒入动画
- cook - 烹饪动画

### 6. 回调完成信号

**reactionDone(int channel) 目标：**
- channel 编码：允许多个并发动画反馈
- 用于长动画序列的完成通知
- 与 Animator.SetTrigger() 或事件系统集成


## 交互流程中的反馈机制

### 反馈链条
1. **用户交互** → ScrollBar/拖拽输入
2. **状态变化** → CutDice.onSlice1ValueChanged() / Stir.OnDragPositionChanged()
3. **阈值判定** → _triggered / _finished 标志
4. **动画播放** → Ingredient_Interaction.playReaction()
5. **完成信号** → CookingManager.onInteractionDone()

### 关键回调链
- Tool.CompleteInteraction() / onInteractionFinished()
- CookingManager.onInteractionDone(bool isFinal)
- Rule输出对象实例化
- DropZone清空和状态重置

## 文件结构与代码分析

### Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs
**职责**: 管理食材的动画反馈和交互响应

```csharp
public class Ingredient_Interaction : MonoBehaviour
{
    InteractionController myController;
    public Animator anim;

    private void Awake()
    {
        anim = GetComponent<Animator>();
    }

    public void playReaction(string animName)
    {
        if(Tools.AnimatorHasParameter(anim, animName))
        {
            anim.Play(animName);
        }
    }

    public void reactionDone(int channel)
    {
        // 反馈完成信号入口
    }
}
```

**关键特性**:
- Animator参数验证 (Tools.AnimatorHasParameter)
- 支持通道化的反馈完成信号
- 最小化设计，为扩展预留空间

### Assets/Scripts/Cooking/Interaction/InteractionController.cs
**职责**: 交互基类，管理容器、工具和食材的生成

```csharp
public class InteractionController : MonoBehaviour
{
    public ContainerTagType usedContainer;
    public ToolData usedTool;
    public List<IngredientData> usedIngredients;

    public Transform IngredientParent;
    public Transform ToolParent;
    public Transform ContainerParent;
    
    public void Initialize(ContainerTagType container, ToolData tool, 
                          List<IngredientData> contentIngredients)
    {
        usedContainer = container;
        usedTool = tool;
        usedIngredients = contentIngredients;
    }
}
```

### Assets/Scripts/Cooking/Tool/CutDice.cs
**职责**: 实现二阶段切割交互，使用ScrollBar进度

```csharp
public class CutDice : InteractionController
{
    [SerializeField] private ScrollBar_Controller slice1;
    [SerializeField] private ScrollBar_Controller slice2;
    [SerializeField] private float triggerThreshold = 0.3f;
    [SerializeField] private float doneThreshold = 0.95f;
    [SerializeField] private float autoDriveDuration = 1f;

    private bool _slice1Triggered, _slice1Done;
    private bool _slice2Triggered, _slice2Done;

    private void OnSlice1ValueChanged(float v)
    {
        if (!_slice1Triggered && v >= triggerThreshold)
        {
            _slice1Triggered = true;
            StartCoroutine(AutoDriveToOne(slice1));
        }
        if (!_slice1Done && v >= doneThreshold)
        {
            _slice1Done = true;
            onSlice1Done();
        }
    }

    private void OnSlice2ValueChanged(float v)
    {
        if (!_slice2Triggered && v >= triggerThreshold)
        {
            _slice2Triggered = true;
            StartCoroutine(AutoDriveToOne(slice2));
        }
        if (!_slice2Done && v >= doneThreshold)
        {
            _slice2Done = true;
            onSLice2Done();
        }
    }

    public void onSlice1Done()
    {
        slice1.gameObject.SetActive(false);
        slice2.gameObject.SetActive(true);
    }

    public void onSLice2Done()
    {
        slice1.gameObject.SetActive(false);
        slice2.gameObject.SetActive(false);
        onInteractionFinished();
    }
}
```

**交互模式**:
1. 第一阶段: slice1.Value >= triggerThreshold → 自动驱动到完成
2. 第一阶段: slice1.Value >= doneThreshold → 激活第二阶段
3. 第二阶段: 重复相同流程
4. 两阶段完成 → onInteractionFinished()

### Assets/Scripts/Cooking/Tool/Stir.cs
**职责**: 实现圆形运动检测的搅拌交互

```csharp
public class Stir : InteractionController
{
    [SerializeField] private DragContainer dragContainer;
    [SerializeField] private MouseDraggableObject draggableObject;
    [SerializeField] private float minTriggerDistance = 5f;
    [SerializeField] private int sampleBufferSize = 30;
    [SerializeField] private float sampleInterval = 0.05f;
    [SerializeField] private float circularAngleThreshold = 270f;
    [SerializeField] private float windingRatioThreshold = 0.3f;

    private float _accumulatedDistance;
    private bool _triggered;
    private bool _finished;
    private List<Vector3> _positionSamples = new List<Vector3>();

    public void OnDragPositionChanged(Vector3 delta)
    {
        if (_finished) return;
        
        _accumulatedDistance += delta.magnitude;
        
        if (!_triggered && _accumulatedDistance >= minTriggerDistance)
        {
            _triggered = true;
        }
    }

    public void CompleteInteraction()
    {
        if (!_triggered || _finished) return;
        _finished = true;
    }

    public bool IsCircularMotion()
    {
        if (_positionSamples.Count < 3) return false;
        
        float cumulativeAbsAngle = 0f;
        float netSignedAngle = 0f;

        for (int i = 1; i < _positionSamples.Count - 1; i++)
        {
            Vector2 v1 = _positionSamples[i] - _positionSamples[i - 1];
            Vector2 v2 = _positionSamples[i + 1] - _positionSamples[i];

            float signedAngle = Vector2.SignedAngle(v1, v2);
            cumulativeAbsAngle += Mathf.Abs(signedAngle);
            netSignedAngle += signedAngle;
        }

        float ratio = Mathf.Abs(netSignedAngle) / cumulativeAbsAngle;
        return ratio >= windingRatioThreshold;
    }
}
```

**交互模式**:
1. 拖拽移动 → 累计距离
2. 距离 >= minTriggerDistance → _triggered = true
3. 外部调用 CompleteInteraction() → _finished = true
4. 圆形运动检测使用绕数算法判定真实圆形vs直线往复


## 3. 文件级分析

### Ingredient_Interaction.cs
**路径**: Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs
**职责**: 食材动画反馈的直接执行器

关键方法:
```csharp
public void playReaction(string animName)
{
    if(Tools.AnimatorHasParameter(anim, animName))
    {
        anim.Play(animName);
    }
}

public void reactionDone(int channel)
{
    // 空实现 - 待扩展的完成回调
}
```

**设计意图**: 该类作为食材与动画系统的桥接,通过Tools.AnimatorHasParameter()进行参数验证,确保动画播放的安全性。reactionDone()是完成信号的扩展点。

### Ingredient_Cooking.cs
**路径**: Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs
**职责**: 食材生命周期管理和放置反馈

核心回调:
- onPlaceRight(): 食材放入区域时触发
- onPickUp(): 食材被拾取时触发  
- onPlaceCancel(): 食材放置取消时触发
- onTossed(): 食材被抛出时触发

与CookingManager的事件链:
- 调用 `CookingManager.onItemReleased()` 
- 调用 `CookingManager.onIngredientInHand()`
- 管理当前所在DropZone的跟踪

### CookingDropZone.cs
**路径**: Assets/Scripts/Cooking/CookingDropZone.cs
**职责**: 容器内容管理和规则匹配触发

关键方法:
- TryAcceptIngredient(ingredient): 验证食材是否符合菜谱规则
- RemoveContent(ingredient): 移除食材
- ClearContents(): 清空所有内容
- NotifyContentsChanged(): 通知管理器内容已改变

内容变化流程:
1. 食材被放入区域 → onDrop()
2. 调用 NotifyContentsChanged()
3. CookingManager.onDropZoneContentsChanged() 被触发
4. 规则解析 → 如果匹配,进入InteractionState

### InteractionController.cs
**路径**: Assets/Scripts/Cooking/Interaction/InteractionController.cs
**职责**: 交互基类,定义工具交互的通用接口

属性:
- usedContainer: 使用的容器类型
- usedTool: 使用的工具数据
- usedIngredients: 食材列表
- IngredientParent, ToolParent, ContainerParent: 生成元素的父节点

Initialize() 方法接收完整的交互上下文。

### Stir.cs (继承 InteractionController)
**路径**: Assets/Scripts/Cooking/Tool/Stir.cs
**职责**: 搅拌交互 - 圆形运动检测

关键特性:
- 圆形运动检测算法(基于缠绕数)
- 位置采样缓冲区(可配置大小和间隔)
- 距离阈值触发(minTriggerDistance)

交互流程:
1. OnDragStart(): 初始化采样
2. OnDragPositionChanged(delta): 累计距离,触发阈值检查
3. IsCircularMotion(): 验证圆形运动
4. CompleteInteraction(): 外部信号完成(动画事件、计时器等)

### CutDice.cs (继承 InteractionController)
**路径**: Assets/Scripts/Cooking/Tool/CutDice.cs
**职责**: 切割交互 - 两阶段滚动条进度

交互阶段:
1. **第一阶段**: slice1 滚动条从0→1
   - 阈值 triggerThreshold: 触发进度
   - 阈值 doneThreshold: 完成进度
   - 自动驱动(AutoDriveToOne)

2. **第二阶段**: slice2 滚动条从0→1
   - 同样的阈值机制
   - 完成后调用 onInteractionFinished()

自动驱动机制:
```csharp
private IEnumerator AutoDriveToOne(ScrollBar_Controller slice)
{
    float rate = (1f - triggerThreshold) / autoDriveDuration;
    while (slice.Value < 1f)
    {
        if (!userDragging)
        {
            slice.SetValue(Mathf.Min(slice.Value + rate * Time.deltaTime, 1f), true);
        }
        yield return null;
    }
}
```

### CookingManager.cs
**路径**: Assets/Scripts/Cooking/CookingManager.cs
**职责**: 烹饪交互的核心协调器

关键状态:
- ingredientChooseState: 选择食材
- toolPlaceState: 放置工具
- InteractionState: 执行交互

关键回调:
```
onItemReleased(ingredient, zone)
  ↓
onDropZoneContentsChanged(zone)
  ↓
规则匹配 → 弹出通用材料UI
  ↓
onToolPlacedIn(tool, zone)
  ↓
规则匹配 → 进入InteractionState
  ↓
(工具交互执行中)
  ↓
onInteractionDone(bool isFinal)
  ↓
消费zone.contents + 实例化rule.output
  ↓
返回ingredientChooseState
```


### Ingredient_Cooking.cs
**路径**: Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs

**关键功能**:
- 继承 SpawnedPlaceable<Ingredient_Cooking>
- 实现食材生命周期管理 (放置、拿起、抛出)
- 与DropZone的交互

**核心回调**:
```csharp
public void onPlaceRight() - 正确放置时触发
public void onPickUp() - 拿起时触发
public void onPlaceCancel() - 取消放置时触发
public void onTossed() - 抛出时触发
```

### Tool_Cooking.cs
**路径**: Assets/Scripts/Cooking/Tool/Tool_Cooking.cs

**关键功能**:
- 工具的生命周期管理
- 与DropZone的交互处理

**核心回调**:
```csharp
public virtual void onAcceptedByZone(DropZone zone)
public virtual void onInteractionDone()
```

### CutDice.cs
**路径**: Assets/Scripts/Cooking/Tool/CutDice.cs

**特点**: 多阶段交互 (两个滑块切割)
```
状态流程:
1. slice1激活 -> 用户拖动至触发阈值 (0.3)
2. 自动驱动至1.0 -> onSlice1Done触发
3. slice1隐藏, slice2显示
4. 用户拖动slice2至触发阈值
5. 自动驱动至1.0 -> onSlice2Done触发
6. 调用onInteractionFinished()
```

### Stir.cs
**路径**: Assets/Scripts/Cooking/Tool/Stir.cs

**特点**: 圆形运动检测
```
关键算法:
- 位置采样: 每0.05秒记录一次位置
- 绕数计算: 通过向量夹角累计判断圆形度
- 触发条件: 累计转角 >= 270° && 净旋转比 >= 0.3
```

### InteractionController.cs
**路径**: Assets/Scripts/Cooking/Interaction/InteractionController.cs

**角色**: 交互基类
```csharp
public ContainerTagType usedContainer
public ToolData usedTool
public List<IngredientData> usedIngredients
public void Initialize(ContainerTagType container, ToolData tool, 
                       List<IngredientData> contentIngredients)
```

### CookingDropZone.cs
**路径**: Assets/Scripts/Cooking/CookingDropZone.cs

**关键功能**:
- 管理区域内的食材内容
- 触发规则检查和反馈

**核心方法**:
```csharp
public bool TryAcceptIngredient(Ingredient_Cooking ingredient)
private void onDrop(Draggable draggable)
public void NotifyContentsChanged()
public void RemoveContent(Ingredient_Cooking ingredient)
public void ClearContents()
```

### CookingManager.cs
**路径**: Assets/Scripts/Cooking/CookingManager.cs

**核心职责**: 全局协调和FSM管理

**关键状态**:
```
IngredientChooseState -> 选择食材
InteractionState -> 执行交互
ResultState -> 显示结果
```

**关键方法**:
```csharp
public void onInteractionDone(bool isFinal)
public void onDropZoneContentsChanged(DropZone zone)
public void onToolPlacedIn(Tool tool, DropZone zone)
```


### Stir.cs - 圆形运动检测交互
**路径**: Assets/Scripts/Cooking/Tool/Stir.cs
**继承**: InteractionController

**核心机制**:
- OnDragPositionChanged(Vector3 delta): 累积拖拽距离，达到 minTriggerDistance 触发交互
- CompleteInteraction(): 外部标记完成（动画事件、计时器等）
- IsCircularMotion(): 绕数算法判断圆形运动（net angle / cumulative angle >= windingRatioThreshold）
- 位置采样缓冲: sampleBufferSize=30, sampleInterval=0.05秒

**关键参数**:
```csharp
[SerializeField] private float minTriggerDistance = 5f;          // 触发阈值距离
[SerializeField] private int sampleBufferSize = 30;              // 采样缓冲大小
[SerializeField] private float sampleInterval = 0.05f;           // 采样间隔
[SerializeField] private float circularAngleThreshold = 270f;    // 累计转角最小值
[SerializeField] private float windingRatioThreshold = 0.3f;     // 圆形运动比例阈值
```

**反馈流程**:
1. 拖拽开始 → OnDragStart() → 清除采样数据
2. 拖拽移动 → OnDragPositionChanged() → 累积距离
3. 距离达阈值 → _triggered = true
4. 圆形运动检测 → IsCircularMotion() 验证
5. 外部完成 → CompleteInteraction() → _finished = true

### CutDice.cs - 多阶段滚动条交互
**路径**: Assets/Scripts/Cooking/Tool/CutDice.cs
**继承**: InteractionController

**核心机制**:
- 两个 ScrollBar_Controller: slice1 和 slice2
- 两阶段流程: 第一刀(slice1) → 第二刀(slice2)
- 自动驱动: AutoDriveToOne() 协程自动将未完成的切割推进至 1.0
- 触发与完成阈值分离: triggerThreshold=0.3, doneThreshold=0.95

**关键参数**:
```csharp
[SerializeField] private float triggerThreshold = 0.3f;    // 触发阈值
[SerializeField] private float doneThreshold = 0.95f;      // 完成阈值
[SerializeField] private float autoDriveDuration = 1f;     // 自动驱动时长
```

**反馈流程**:
1. slice1.Value >= triggerThreshold → _slice1Triggered = true → StartCoroutine(AutoDriveToOne)
2. 自动驱动或用户拖拽至 >= doneThreshold → _slice1Done = true → onSlice1Done()
3. onSlice1Done() → 隐藏slice1, 显示slice2
4. slice2.Value >= doneThreshold → _slice2Done = true → onSLice2Done()
5. onSLice2Done() → 隐藏所有UI → onInteractionFinished()

### InteractionController.cs - 基类
**路径**: Assets/Scripts/Cooking/Interaction/InteractionController.cs
**作用**: CutDice、Stir 的基类，提供交互框架

**继承关系**:
```
MonoBehaviour
  └── InteractionController (CutDice、Stir 的基类)
      └── [包含容器、工具、食材的数据引用]
```

**核心成员**:
```csharp
public ContainerTagType usedContainer;
public ToolData usedTool;
public List<IngredientData> usedIngredients;
public Transform IngredientParent;
public Transform ToolParent;
public Transform ContainerParent;
```

### CookingDropZone.cs - 容器与内容管理
**路径**: Assets/Scripts/Cooking/CookingDropZone.cs
**关键职责**: 管理容器内容，验证规则，触发内容变更通知

**核心方法**:
```csharp
public bool TryAcceptIngredient(Ingredient_Cooking ingredient)
  → 根据 CookingRule 验证食材是否能放入

public void onDrop(IDraggable draggable)
  → 路由到 Tool 或 Ingredient 处理逻辑

public void NotifyContentsChanged()
  → 触发 CookingManager.onDropZoneContentsChanged()
  → 触发规则解析和材料弹窗管理
```

**内容管理**:
- RemoveContent(ingredient)
- RestoreContent()
- ClearContents() → 消费容器内容

### CookingManager.cs - 核心编排类
**路径**: Assets/Scripts/Cooking/CookingManager.cs
**关键职责**: FSM 状态管理、交互链调度、回调分派

**FSM 状态**:
```
IngredientChooseState
  ↓ (选择食材)
PlaceInContainerState
  ↓ (放入容器)
ToolChooseState
  ↓ (选择工具)
InteractionState (存储 contextRule, contextZone, contextTool)
  ↓ (执行交互)
IngredientChooseState (循环)
```

**关键回调**:
- onInteractionDone(bool isFinal): 消费容器内容，生成输出食材，清空容器
- onDropZoneContentsChanged(zone): 规则解析，弹窗管理
- onToolPlacedIn(tool, zone): 规则验证，进入交互状态
- onIngredientInHand(data, source): 容器标签验证，数量限制检查


### CutDice.cs - 多阶段切割交互
**路径**: Assets/Scripts/Cooking/Tool/CutDice.cs

**核心特性**:
- 双阶段ScrollBar交互（slice1, slice2）
- 阈值驱动：triggerThreshold (0.3) 和 doneThreshold (0.95)
- AutoDriveToOne()协程自动推进未完成的阶段
- onSlice1Done(): 切换到第二个ScrollBar
- onSLice2Done(): 完成交互，调用onInteractionFinished()

**代码段**:
```csharp
private void OnSlice1ValueChanged(float v)
{
    if (!_slice1Triggered && v >= triggerThreshold)
    {
        _slice1Triggered = true;
        StartCoroutine(AutoDriveToOne(slice1));
    }
    if (!_slice1Done && v >= doneThreshold)
    {
        _slice1Done = true;
        onSlice1Done();
    }
}

public void onSLice2Done()
{
    slice1.gameObject.SetActive(false);
    slice2.gameObject.SetActive(false);
    onInteractionFinished();
}
```

### CookingManager.cs - 中央编排器
**路径**: Assets/Scripts/Cooking/CookingManager.cs

**关键方法**:
- onInteractionDone(bool isFinal): 消费zone.contents，实例化rule.output，清除zone，返回ingredientChooseState
- onDropZoneContentsChanged(zone): 触发规则解析和通用材料弹窗管理
- onToolPlacedIn(tool, zone): 解析规则并进入InteractionState
- onIngredientInHand(data, source): 验证容器标签并检查数量限制

**FSM状态**:
- ingredientChooseState: 选择食材阶段
- InteractionState: 存储contextRule, contextZone, contextTool
- 状态机通过Setup()方法注入上下文

### InteractionController.cs - 基础交互控制
**路径**: Assets/Scripts/Cooking/Interaction/InteractionController.cs

**设计目的**: 作为所有交互类的基类（Stir, CutDice继承）
**属性**:
- usedContainer, usedTool, usedIngredients: 数据源
- IngredientParent, ToolParent, ContainerParent: 生成位置
- relativePath, IngredientPath, ToolPath, ContainerPath: 资源路径

### Ingredient_Interaction.cs - 食材反馈控制
**路径**: Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs

**核心功能**:
- playReaction(string animName): 触发特定的动画反应
- reactionDone(int channel): 完成回调（当前为空，待扩展）
- 使用Tools.AnimatorHasParameter()进行参数验证

**代码段**:
```csharp
public void playReaction(string animName)
{
    if(Tools.AnimatorHasParameter(anim, animName))
    {
        anim.Play(animName);
    }
}

public void reactionDone(int channel)
{
    // 完成回调 - 待扩展为实际反馈逻辑
}
```

## 4. 多阶段动画模式分析

### ScrollBar驱动的多阶段（CutDice模式）
```
用户拖动 → ScrollBar值增加
  ↓
triggerThreshold检查 → 触发交互
  ↓
AutoDriveToOne()自动推进
  ↓
doneThreshold检查 → 阶段完成
  ↓
onSlice1Done() / onSLice2Done()切换到下一阶段或完成
```

### 外部驱动的完成（Stir模式）
```
用户拖动 → 圆形运动检测
  ↓
距离累计 → 触发阈值
  ↓
CompleteInteraction()外部调用（动画事件/计时器）
  ↓
IsCircularMotion()验证圆形运动
  ↓
onInteractionFinished()完成交互
```

### 动画参数传递
```
Ingredient_Interaction.playReaction(animName)
  ↓
Tools.AnimatorHasParameter(anim, animName)验证
  ↓
anim.Play(animName)执行动画
  ↓
reactionDone(int channel)完成回调
```

## 5. 反馈机制总结

### 事件驱动反馈链
1. **交互完成触发**: Tool.onInteractionDone()
2. **管理器响应**: CookingManager.onInteractionDone(isFinal)
3. **规则执行**: rule.output实例化
4. **食材反馈**: Ingredient_Interaction.playReaction()
5. **完成确认**: reactionDone()回调

### 设计原则
- 使用**事件回调**而非async/await
- 状态通过**FSM**管理
- 多阶段进度通过**阈值检查**驱动
- 动画通过**Animator参数**控制
- 完成通过**外部方法调用**确认

## 6. Ingredient_Interaction设计建议

基于上述分析，Ingredient_Interaction应包括：

1. **反馈播放方法**:
   - playReaction(string animName): 已实现
   - 需扩展支持多阶段反馈序列

2. **完成回调**:
   - reactionDone(int channel): 待实现
   - 应与CookingManager.onInteractionDone()协调

3. **动画状态管理**:
   - 跟踪当前播放的动画
   - 支持动画队列（用于多步骤反馈）

4. **与交互系统的集成**:
   - 通过CookingDropZone接收反馈触发
   - 通过CookingManager获取交互上下文

5. **扩展可能性**:
   - 支持多通道反馈（已预留channel参数）
   - 支持条件反馈（基于rule和zone context）


## 4. 动画控制与多阶段交互模式

### Ingredient_Interaction 中的动画播放
- **playReaction(string animName)**: 触发Animator指定动画
- **参数验证**: 使用Tools.AnimatorHasParameter()确保参数存在
- **reactionDone(int channel)**: 动画完成回调（当前为空）

### 多阶段动画处理模式

#### CutDice 的分阶段切割
1. **第一阶段 (Slice 1)**
   - slice1.onValueChanged 监听滚动条值
   - triggerThreshold (0.3) 触发 onSlice1Done()
   - 触发后自动驱动至 doneThreshold (0.95)

2. **第二阶段 (Slice 2)**
   - onSlice1Done() 禁用slice1，激活slice2
   - slice2 重复相同的阈值流程
   - 完成后调用 onInteractionFinished()

#### Stir 的外部完成机制
- OnDragPositionChanged(): 累计距离达到 minTriggerDistance 时触发
- CompleteInteraction(): 外部方法，用于动画事件/计时器调用
- IsCircularMotion(): 验证圆形运动（绕数算法）

### 反馈链路
动画播放 → 完成回调 → CookingManager.onInteractionDone() → 规则解析 → 产物生成


## 5. 推荐的 Ingredient_Interaction 设计模式

### 当前实现状态
当前 Ingredient_Interaction.cs 实现极小：
- 仅包含 playReaction(string animName) 用于触发动画
- reactionDone(int channel) 为空回调存根

### 推荐扩展方向

#### A. 事件驱动反馈链
```csharp
public class Ingredient_Interaction : MonoBehaviour
{
    public event System.Action<string> onReactionStarted;
    public event System.Action<string> onReactionCompleted;
    
    public void playReaction(string animName, bool autoComplete = false)
    {
        if(Tools.AnimatorHasParameter(anim, animName))
        {
            anim.Play(animName);
            onReactionStarted?.Invoke(animName);
            
            if(autoComplete)
            {
                StartCoroutine(WaitForAnimationComplete(animName));
            }
        }
    }
    
    private IEnumerator WaitForAnimationComplete(string animName)
    {
        AnimatorStateInfo stateInfo = anim.GetCurrentAnimatorStateInfo(0);
        yield return new WaitForSeconds(stateInfo.length);
        reactionDone(0);
    }
    
    public void reactionDone(int channel)
    {
        onReactionCompleted?.Invoke(currentReaction);
        // 通知 CookingManager 反馈完成
    }
}
```

#### B. 多通道反馈系统
- channel 0: 主要反馈（食材变色、变形）
- channel 1: 次要反馈（粒子效果、声音）
- channel 2: 完成标志（动画完成、UI 更新）

#### C. 与 CookingManager 的集成
```
Ingredient_Interaction.reactionDone(channel)
  ↓
CookingManager.onIngredientReactionComplete(ingredient, channel)
  ↓
更新 UI / 进行下一步
```


### 设计建议

#### 1. 事件驱动的反馈机制
```csharp
// 推荐模式：事件回调链
public class Ingredient_Interaction : MonoBehaviour
{
    public event System.Action<string> onReactionStarted;
    public event System.Action onReactionCompleted;
    
    public void playReaction(string animName)
    {
        if(Tools.AnimatorHasParameter(anim, animName))
        {
            onReactionStarted?.Invoke(animName);
            anim.Play(animName);
        }
    }
    
    public void reactionDone(int channel)
    {
        onReactionCompleted?.Invoke();
        // 通知CookingManager交互完成
    }
}
```

#### 2. 上下文管理
- 在CookingPhaseState的InteractionState中维护交互上下文
- 将rule、zone、tool一起传入Ingredient_Interaction
- 完成交互时回调CookingManager.onInteractionDone()

#### 3. 动画同步
- 使用AnimationEvent触发reactionDone()回调
- 或在动画完成时手动调用CompleteInteraction()方法
- 对于多阶段动画，逐阶段播放而非一次性播放

## 总结

Ingredient_Interaction系统的核心：
1. **动画播放**：通过Animator.Play()控制食材反应
2. **反馈链**：playReaction() → 动画 → reactionDone() → CookingManager
3. **状态协调**：通过InteractionState维护上下文
4. **多阶段支持**：支持动画序列和渐进式完成


#### 2. 多阶段交互的实现
```csharp
// 模板：ScrollBar 多阶段模式（参考 CutDice）
public void OnStageValueChanged(float value)
{
    if (!_stageTriggered && value >= triggerThreshold)
    {
        _stageTriggered = true;
        OnStageTriggered();  // 调用反馈
    }
    if (!_stageDone && value >= doneThreshold)
    {
        _stageDone = true;
        OnStageDone();  // 完成反馈
    }
}

private void OnStageDone()
{
    // 更新UI、动画、或进入下一阶段
    myController.playReaction("next_stage_animation");
}
```

#### 3. 事件驱动架构
```csharp
// Ingredient_Interaction 应实现
public class Ingredient_Interaction : MonoBehaviour
{
    public event System.Action<string> OnReactionStarted;
    public event System.Action<int> OnReactionCompleted;
    
    public void playReaction(string animName)
    {
        if(Tools.AnimatorHasParameter(anim, animName))
        {
            anim.Play(animName);
            OnReactionStarted?.Invoke(animName);
        }
    }
    
    // 由动画事件回调
    public void reactionDone(int channel)
    {
        OnReactionCompleted?.Invoke(channel);
    }
}
```

### 关键设计原则

1. **单一责任**：Ingredient_Interaction 仅负责动画播放和反馈信号，不处理逻辑决策
2. **事件驱动**：使用 C# 事件而非直接方法调用，以解耦交互层和逻辑层
3. **参数验证**：始终使用 Tools.AnimatorHasParameter() 验证动画参数存在性
4. **多阶段支持**：设计 playReaction() 支持任意阶段标识符，易于扩展
5. **反馈链完整**：确保 reactionDone() 被妥善回调，触发下游逻辑

## 6. 核心技术要点总结

| 特性 | 实现方式 | 文件位置 |
|------|--------|--------|
| 动画控制 | Animator.Play() + 参数验证 | Ingredient_Interaction.cs |
| 多阶段交互 | ScrollBar 阈值检测 | CutDice.cs |
| 圆形运动检测 | 绕数算法 + 位置采样 | Stir.cs |
| 状态管理 | FSM（Idle/Placing） | SpawnedPlaceable.cs |
| 反馈链条 | 方法回调链 + 事件系统 | CookingManager.cs |
| 规则匹配 | ToolTagResolver 交集 | ToolTagResolver.cs |
| 内容管理 | DropZone contents List | CookingDropZone.cs |
| 上下文传递 | InteractionState Setup() | CookingPhaseState.cs |

## 7. 推荐的下一步行动

1. **完善 Ingredient_Interaction**：
   - 实现事件系统（OnReactionStarted、OnReactionCompleted）
   - 支持多反应阶段（reaction_slice1、reaction_stir 等）
   - 添加反馈日志以便调试

2. **扩展反馈机制**：
   - 为每个工具类型（Stir、CutDice 等）定制反馈动画
   - 集成粒子效果和声音反馈
   - 实现进度可视化（进度条、数字计数等）

3. **测试与验证**：
   - 验证所有反馈链条的完整性
   - 测试多材料混合时的反馈时序
   - 检查边界情况（快速交互、重复交互等）

4. **性能优化**：
   - 池化动画 Animator 组件以减少 GetComponent 开销
   - 缓存 AnimatorHasParameter 结果以加速参数验证

---

**报告生成日期**: 2026年6月21日
**分析范围**: Assets/Scripts/Cooking/ 及相关 UI 交互系统
**状态**: 完成

#### 3. 动画控制与状态同步
```csharp
// 推荐实现方式
private Dictionary<string, InteractionState> _interactionStates = new Dictionary<string, InteractionState>();

public void PlayReactionWithCallback(string animName, System.Action onComplete)
{
    if (Tools.AnimatorHasParameter(anim, animName))
    {
        anim.SetTrigger(animName);
        StartCoroutine(WaitForAnimationComplete(animName, onComplete));
    }
}

private IEnumerator WaitForAnimationComplete(string animName, System.Action onComplete)
{
    yield return null; // 等待一帧使动画开始
    AnimatorStateInfo stateInfo = anim.GetCurrentAnimatorStateInfo(0);
    float animLength = stateInfo.length / stateInfo.speed;
    yield return new WaitForSeconds(animLength);
    onComplete?.Invoke();
}
```

#### 4. 反馈事件系统集成
```csharp
public class IngredientInteractionFeedbackSystem
{
    public event System.Action<Ingredient_Cooking, string> OnReactionStarted;
    public event System.Action<Ingredient_Cooking, string> OnReactionCompleted;
    public event System.Action<Ingredient_Cooking, int> OnChannelNotified;

    public void NotifyReactionStarted(Ingredient_Cooking ingredient, string animName)
    {
        OnReactionStarted?.Invoke(ingredient, animName);
    }

    public void NotifyReactionCompleted(Ingredient_Cooking ingredient, string animName)
    {
        OnReactionCompleted?.Invoke(ingredient, animName);
    }

    public void NotifyChannel(Ingredient_Cooking ingredient, int channel)
    {
        OnChannelNotified?.Invoke(ingredient, channel);
    }
}
```

## 6. 关键发现与建议总结

### 核心设计模式
1. **FSM 架构**: 使用状态机管理交互生命周期，提供清晰的状态转换和上下文管理
2. **事件驱动**: 通过事件和回调链实现松耦合的交互反馈
3. **动画驱动**: Animator 组件与交互逻辑紧密结合，支持参数验证和多阶段控制
4. **上下文注入**: InteractionState 存储和注入交互上下文（规则、区域、工具）

### Ingredient_Interaction 设计基础
- **核心职责**: 管理食材的动画反应和反馈控制
- **关键方法**: playReaction()、reactionDone()、可选的多阶段控制方法
- **集成点**: 与 CookingManager、InteractionController、动画事件系统的集成
- **扩展方向**: 支持链式反应、条件反馈、多种食材特殊效果

### 技术债务
1. reactionDone(int channel) 方法当前为空，需要实现
2. Tool 类中的回调存在重复定义，可统一为基类实现
3. 动画事件无统一的触发机制，建议使用 Animator Event 回调

### 建议的后续实现步骤
1. **第一阶段**: 实现基础的 playReaction() 和 reactionDone() 逻辑
2. **第二阶段**: 集成事件系统和反馈链条
3. **第三阶段**: 支持多阶段动画和条件反馈
4. **第四阶段**: 优化性能并添加特殊效果支持

## 7. 相关文件路径和代码引用

### 主要交互类
- **Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs** - 本体实现
- **Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs** - 食材管理
- **Assets/Scripts/Cooking/CookingManager.cs** - 中央协调
- **Assets/Scripts/Cooking/Interaction/InteractionController.cs** - 基类
- **Assets/Scripts/Cooking/Tool/Stir.cs** - 搅拌交互示例
- **Assets/Scripts/Cooking/Tool/CutDice.cs** - 切割交互示例
- **Assets/Scripts/Cooking/CookingPhaseState.cs** - 状态管理
- **Assets/Scripts/Cooking/CookingDropZone.cs** - 区域管理
- **Assets/Scripts/MouseInteractive/Drag/SpawnedPlaceable.cs** - 放置逻辑

### 动画和反馈相关
- **Animator Parameter Validation**: Tools.AnimatorHasParameter()
- **Event System**: CookingManager 的事件回调链
- **Coroutine Patterns**: AutoDriveToOne(), WaitForAnimationComplete()

## 8. 实现指南

### 食材反应流程
```
用户交互触发 (CutDice/Stir) 
    ↓
InteractionController.onInteractionTriggered()
    ↓
CookingManager.contextRule/contextZone/contextTool 设置
    ↓
CookingManager.onInteractionDone()
    ↓
Ingredient_Interaction.playReaction(animName)
    ↓
Animator.Play(animName)
    ↓
Animation Event 触发 reactionDone(channel)
    ↓
反馈链条继续...
```

## 结论

本报告详细分析了 ButtonProject 中食材交互和反馈系统的当前实现。通过理解 FSM 架构、事件驱动模式、动画控制机制和反馈链条，为 Ingredient_Interaction 类的设计提供了坚实的基础。建议采用事件驱动的反馈机制、支持多阶段交互、并与现有的 CookingManager 和 InteractionController 系统紧密集成。

---

**报告生成日期**: 2026-06-21
**分析范围**: ButtonProject 食材交互系统
**关键文件**: 9+ 主要类文件，涉及交互、动画、状态管理和事件系统

## 6. 关键设计决策总结

### 状态追踪
- 使用 `_triggered` 和 `_finished` 标志追踪交互状态
- 避免重复触发和完成回调
- 清晰的状态转换流程

### 事件链路
- Tool 完成交互 → CookingManager.onInteractionDone()
- 消费 zone.contents → 实例化 rule.output
- 更新UI和游戏状态

### 动画与交互同步
- Animator.Play() 触发视觉反馈
- 外部回调（如动画事件）标记完成
- ScrollBar 阈值自动推进进度

## 7. 实现清单

实现 Ingredient_Interaction 的完整功能需要：

1. **基础动画控制**
   - [ ] 在 playReaction() 中添加参数验证
   - [ ] 支持多个 Animator 参数（如 "Diced", "Stirred", "Cooked"）
   - [ ] 添加动画完成检测

2. **反馈机制集成**
   - [ ] 从 Tool 接收完成回调
   - [ ] 广播状态变化事件
   - [ ] 连接到 CookingManager 的状态机

3. **多阶段交互支持**
   - [ ] 支持顺序化的多步骤处理
   - [ ] 在每个阶段之间等待用户交互完成
   - [ ] 管理阶段之间的状态转换

4. **UI 反馈整合**
   - [ ] 显示当前交互进度
   - [ ] 提供视觉确认（动画、粒子效果）
   - [ ] 同步 DropZone 内容显示

## 8. 相关文件快速参考

| 文件路径 | 主要职责 | 关键方法 |
|---------|--------|---------|
| Ingredient_Interaction.cs | 动画播放和反馈 | playReaction(), reactionDone() |
| Ingredient_Cooking.cs | 食材放置和状态管理 | onPlaceRight(), onPickUp() |
| InteractionController.cs | 交互基类和初始化 | Initialize() |
| Stir.cs | 圆形运动检测 | IsCircularMotion(), CompleteInteraction() |
| CutDice.cs | 多阶段切割交互 | OnSlice1ValueChanged(), onSlice1Done() |
| Tool_Cooking.cs | 工具生命周期管理 | onAcceptedByZone(), onInteractionDone() |
| CookingDropZone.cs | 容器内容管理 | TryAcceptIngredient(), NotifyContentsChanged() |
| CookingManager.cs | 全局协调和状态机 | onInteractionDone(), onDropZoneContentsChanged() |
| CookingPhaseState.cs | 交互上下文管理 | Setup(), EnterState() |

## 9. 结论

Ingredient_Interaction 系统的设计基础包括：

1. **清晰的反馈链**：Tool → Manager → Ingredient → Animation
2. **状态驱动的交互**：每个交互都有明确的触发和完成条件
3. **灵活的动画系统**：支持多个参数和多阶段动画
4. **事件驱动的架构**：通过回调和事件完成松耦合通信

通过理解这些机制，可以为 Ingredient_Interaction 设计一个强大且可扩展的反馈系统，支持复杂的食材处理流程。

---

**文档生成时间**: 2026-06-21
**项目**: ButtonProject - 食材交互系统
**分析范围**: 15+ 个交互相关的核心类文件


### 交互上下文传递
- InteractionState 在进入前通过 Setup() 方法注入 rule, zone, tool
- CookingManager.onInteractionDone() 可以访问完整的交互上下文
- 支持动态规则匹配和结果生成

### 性能考虑
- 圆形运动检测使用固定大小的采样缓冲（30个样本）
- 多阶段交互通过阈值（threshold）触发，避免帧级检测
- 事件驱动架构减少轮询开销

## 7. 总结与建议

### 核心发现

1. **反馈机制的本质**
   - 不是异步的长流程，而是同步的事件链
   - 每个交互工具负责自己的状态管理和完成信号
   - CookingManager 中央协调但不控制工具的执行流程

2. **食材的角色**
   - Ingredient_Cooking 负责容器追踪和拾起/放下生命周期
   - Ingredient_Interaction 负责动画播放和视觉反馈
   - 两者通过事件系统解耦

3. **动画与状态同步**
   - 使用 Animator.Play() 立即播放，支持参数检查
   - 多阶段交互通过阈值或外部完成调用推进
   - 可选：使用 AnimatorStateInfo 监控动画进度

### 建议的 Ingredient_Interaction 实现方向

```csharp
public class Ingredient_Interaction : MonoBehaviour
{
    [SerializeField] private Animator anim;
    [SerializeField] private ParticleSystem feedbackParticles;
    [SerializeField] private AudioSource feedbackAudio;
    
    // 事件系统
    public event System.Action<string> OnReactionStarted;
    public event System.Action<string> OnReactionFinished;
    
    // 生命周期
    private void Awake()
    {
        anim = GetComponent<Animator>();
    }
    
    // 主要接口
    public void PlayReaction(string animName)
    {
        if (Tools.AnimatorHasParameter(anim, animName))
        {
            anim.Play(animName);
            OnReactionStarted?.Invoke(animName);
            StartCoroutine(MonitorAnimationState(animName));
        }
    }
    
    // 动画完成监控
    private IEnumerator MonitorAnimationState(string animName)
    {
        // 等待动画完成
        yield return null;
        
        AnimatorStateInfo stateInfo = anim.GetCurrentAnimatorStateInfo(0);
        while (anim.GetCurrentAnimatorStateInfo(0).IsName(animName))
        {
            yield return null;
        }
        
        OnReactionFinished?.Invoke(animName);
    }
    
    // 外部完成信号
    public void ReactionDone(int channel)
    {
        OnReactionFinished?.Invoke($"channel_{channel}");
    }
}
```

### 集成步骤

1. 在 CookingManager 的交互完成处理中调用 ingredient.PlayReaction()
2. 订阅 OnReactionFinished 以更新 UI 或触发下一步
3. 配合工具的进度事件（如 Stir 的距离累计）实现并行反馈
4. 使用粒子系统和音效增强交互的视觉和听觉反馈

## 文件路径速查表

| 文件 | 路径 |
|------|------|
| Ingredient_Interaction | Assets/Scripts/Cooking/Ingredient/Ingredient_Interaction.cs |
| Ingredient_Cooking | Assets/Scripts/Cooking/Ingredient/Ingredient_Cooking.cs |
| CookingManager | Assets/Scripts/Cooking/CookingManager.cs |
| InteractionController | Assets/Scripts/Cooking/Interaction/InteractionController.cs |
| Stir | Assets/Scripts/Cooking/Tool/Stir.cs |
| CutDice | Assets/Scripts/Cooking/Tool/CutDice.cs |
| Tool_Cooking | Assets/Scripts/Cooking/Tool/Tool_Cooking.cs |
| CookingDropZone | Assets/Scripts/Cooking/CookingDropZone.cs |
| CookingPhaseState | Assets/Scripts/Cooking/CookingPhaseState.cs |
| SpawnedPlaceable | Assets/Scripts/MouseInteractive/Drag/SpawnedPlaceable.cs |
| ToolTagResolver | Assets/Scripts/Cooking/Tool/ToolTagResolver.cs |

---

**报告生成时间**: 2026-06-21  
**项目**: ButtonProject - 食材交互与反馈系统  
**目标**: 为 Ingredient_Interaction 设计提供基础分析

