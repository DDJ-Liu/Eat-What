# 《是啊，吃什么》项目整体阅读理解与推进基线

最后结构一致性复核：2026-09-17。已对照源码入口、Build Settings、Packages、关键场景/Prefab/生成数据、Resources 与 Git 差异，并只读检查 Unity 现场。当前 P0 场景处于 Edit、clean，Console Error=0。本次不重做全部 Play Mode，既有机器与人工验收以正式任务结算为准。

最后专项核验：2026-09-22（MIG63 E1/H及MIG-F06）。用户确认37bb854人工终验通过，main与unity6.3-baseline已集成同一候选，backup保持起点。原工作区content已在6000.3.24f1完成H接棒，TMP84/LFS、9组离线、5组Play及4项长期探针通过，业务冻结解除。其后独立定向重序列化处理3场景/11Prefab，仅3场景有磁盘变化；7934项对象/设置记录一致，meta/GUID、旧参考场景和字体未变，格式更新后的5组Play与Windows构建通过。后续格式提交留在content，未移动E1标签。详情见[E1/H报告](.ai-workspace/MIG63/E1H_接棒与后续项.md)。完整结构复核日期仍2026-09-17，未新增全项目人工视觉验收。

2026-09-20资产移交审查及后续清理：用户已人工核对全部缺口资产，确认无额外问题，交付状态按“完成”记录；初审匹配差异保留为历史证据，不再作为待补交事项。按用户要求在 main 上将根 LICENSE 原文纳入 Git 跟踪，删除 Assets 内 14 张已确认的《明日方舟》角色图片及 meta，清理 3 个 Prefab 的图片引用/角色名称，将 Dialog.dat 的 TestDialog 12 行旧测试数据换为 1 行中性占位，两组顾客点餐表保持不变。用户明确 CookingPrepare 已弃用；其场景文件和现有 Build 列表未改，当前 P0/P1 场景已有修改保持原样。已复查图片哈希、GUID、文本、3 个 DAT 和 Scripts.rar；Unity 刷新后 Console Error=0，活动场景 Edit/clean，未做 Play Mode 或新构建。合同外包/商业发行授权边界及 Spine、Text Animator、字体等许可问题仍按[资产移交审查报告](.ai-workspace/outputs/control/资产转让审查_20260920/审查报告.md)单独管理；完整操作与范围见[后续处理记录](.ai-workspace/outputs/control/资产转让审查_20260920/后续清理/处理记录.md)。不改变阶段人工验收结论及2026-09-17完整结构复核日期。

本文件保留原入口名并整理为当前基线；整理前全文和逐项过程保存在[阶段归档](.ai-workspace/archive/2026-09-17/README.md)。源码与场景导航见[CODEBASE_MAP](CODEBASE_MAP.md)。

## 1. 本阶段完成情况

用户于2026-09-17确认本轮人工审查全部通过。AI-000001～145 全部关闭：137 VERIFIED、8 CLOSED_WITHOUT_ACCEPTANCE。8项保持过时/被覆盖或免除独立技术人审的原性质，不改记人工通过；原错误、拒绝、修订与恢复历史完整保留。

本轮结算覆盖：
- ShortCycle P0/P1 的 Session/FSM、Director/Space/Set、层栈、Modal 与表现接管。
- 正式菜谱封面资产、Cover/目录/浏览显隐和既有 CurrentRecipeTab 规则。
- 0.45秒线性镜头运动、空曲线容错、中途返向/取消及输入锁。
- 冰箱猫整体随动、6层/30槽、五列命中、真实鼠标方向/边界/焦点与重入、名称与数量标签修复。
- Schema v2.4 业务规则、配表/生成资产、Catalog 容量绑定与工具化滚动。
- 逐组描边/阴影合并、成员独立开关、HDR Alpha、泡泡视效和两类 Lab。
- 面向配置人员的中文组件说明。

这标志着已安排的阶段任务链闭合，不代表完整“四菜 → 最终成菜 → 评分 → 变体保存 → 长循环”产品已经完成。

## 2. 当前运行入口与事实来源

| 入口 | 定位 |
| --- | --- |
| Assets/Scenes/Cooking/ShortCycle_P0P1.unity | 本阶段开发与人工验收场景 |
| Assets/Scenes/ToolTests/VisualEffectsLab.unity | 描边/阴影/泡泡统一试验入口 |
| Assets/Scenes/ToolTests/FridgeScrollLab.unity | 滚轮独立模块与普通内容样例 |
| CookingPrepare.unity、CookingProcess.unity | E1基线与接棒目录均以ShortCycle为index0、Process为index1；Prepare保留文件但移出Build，两个旧场景仍禁止随布局批次改写 |
| DataTables/、Assets/Generated/DataTables/ | 配表权威源与 Unity 生成结果 |
| 组件说明文档/ | 配置人员操作入口 |
| .ai-workspace/runtime/queue-state.json | 本机任务权威状态，保留全部145项与下一个编号146 |
| .ai-workspace/archive/2026-09-17/ | 完整历史、原路径映射、哈希及结算索引 |

已跟踪项目内容有249个 Assets/Scripts C#、63个 Assets/Editor C#（含4个长期迁移验证文件，不含H临时入口）、Assets/Scenes下15个场景，Prefabs/Resources共71个Prefab；14个schema，生成目录122个asset（含共享资源和catalog）。第三方示例场景不混入正式场景目录统计。

## 3. 现有架构

ShortCycle 以 SessionManager 为唯一场景入口，数据从 CK01GeneratedDataCatalog 解析；状态机通过 IShortCyclePresentationHost 调 Director，由 Director 编排 Space、Set、Modal、Traveler、Tray 和 CameraPan。正式场景使用世界空间 Sprite/TMP；不能重新引入已经撤销的 AI41 平行静态层作为功能树。

输入继续复用 InputManager / MouseManager / MouseInteractionLayer。整猫滚动经 ShortCycleScrollAdapter 映射物理方向，最终复用 ScrollArea_Controller、DragContainer 和 DragLimit；普通内容可以独立使用通用滚动工具。

描边参数来源与参与合并分开。合并使用逐组像素遮罩和显式离屏绘制，普通排序宿主回画面，每组两RT；泡泡使用显式后景捕获与局部折射。新视觉改动继续先在统一 Lab 验证。

数据使用14表CSV/schema与统一导入器，业务约束已到v2.4。Schema JSON的格式版本仍为2。unknown_product_item解析为prd_unknown；demo容量档fcap_lv2解析为30。正式导表与旧Resources字符串链并存，改名要同时检查GUID、Importer和字符串契约。

## 4. 尚未开发完成或需下一轮明确的边界

| 边界 | 当前判断 |
| --- | --- |
| ShortCycle → 完整P2业务 | 已有携带菜谱ID和备菜内容的DataHandoff；TransitionAdapter仅发Requested事件，不能视为已完成跨场景消费与连续加工闭环 |
| 最终成菜/评分/变体持久化 | 旧CookingManager.isFinal仍是注释入口；完整结果页、评分、保存和重开一轮需要后续开发 |
| 旧房间上下文 | onChooseRoom仍只写房间ID，未由本轮统一改造；下一轮涉及P2时须核实调用链 |
| 筛选/后续F链 | ApplyFilterView有方法与单测，生产调用方未接；shortcycle.fridge.filter仍为后续段桩 |
| 全工程_TUNE终调审查 | 用户明确要求暂不安排，本次不创建任务 |
| 未来素材/长循环角色素材 | 保持原搁置决定，不自动导入或配置 |
| 构建入口 | ShortCycle为Windows x64启动入口、Process为index1，Prepare及Lab不在正式Build。M4构建/场景往返和用户终验通过，E1已集成；原工作区H运行验证及格式更新后构建通过 |
| 换机器还原 | Packages已纳入Git，但MCP仍使用本机外部路径；准备环境时要处理该依赖 |

旧Prepare/Process中其它历史风险详见归档中的原基线。它们是后续定位线索；没有本轮重新核验的条目，不标成现存新故障或已修复，更不自动排入下一轮。

## 5. 验证与可信度

本阶段已有Unity运行、GPU、截图和用户手动结论；人工通过覆盖此次提交的已安排任务范围。阶段整理额外执行归档全文件哈希、固定夹具对照、既有离线数据/短循环/渲染/工作流回归，详细输出见 .ai-workspace/outputs/control/阶段整理_20260917/。

离线桩与静态审计不能替代真实鼠标、GPU和画面验收。当前没有独立业务NUnit测试程序集或完整CI构建管线；Assets/Tests是视效夹具。不得把“有测试脚本”写成“所有Unity运行路径自动覆盖”。

不因阶段归档移除Unity内G26探针、人工驱动器、Lab、Prefab或生产资产。人工验收后字体当前现场应作为新基线记录，不拿历史204字备份自动覆盖当前文件。

## 6. 下一轮承接

当前MANUAL，监管与四条领取日程暂停；独立美术午夜审查保持原配置。用户已启动 MIG63，采用独立手动台账，不占用或重置 AI146，不自动启动新业务批次。

建议下一轮明确一个可验收目标及其入口，再按代码、引擎、素材依赖安排；若推进P2，应先核实DataHandoff的实际消费者、会话/库存所有权、规则执行和最终成菜落点。此处仅为承接建议，不构成已排任务或批准的新范围。

用户已决定并于2026-09-21启动迁移至 Unity 6.3 LTS，本批次锁定6000.3.24f1，Cinemachine保持2.x，官方AI工具链后续另评估。执行依据为[MIG63迁移基线](.ai-workspace/MIG63/迁移基线.md)，暂估6–10人日、累计12人日仍未闭合时重新评估。主要风险为Spine 3.8、TMP/Text Animator及自制离屏视效。Spine实际用于Spine Sample和HorizontalPlayerControllerTest，两处未来长循环预研必须保留并回归；不能因ShortCycle未引用而移除。迁移尚未完成，不把准备阶段的验证当作Unity 6兼容结论。

MIG63启动对照发现：Spine Sample与HorizontalPlayerControllerTest在2022已有骨骼读取失败；Sample/output4头部3.8.75被现有runtime显式拒绝，随后CharacterEquipment空引用。用户最新决定旧素材难以溯源，改用现有Tomato 3.8.99测试及作为后续参考；旧数据和两场景保留，缺陷登记为迁移前MIG-P01，不等待重导出阻断E1。Tomato真实渲染、动画、移动、换装仍为必要回归，不能以保留文件代替功能验证。

美术导入继续先逐文件识别/比对/最终路径命名草案，经用户审核后导入；视效更新同任务维护组件使用说明。流水线自身临时脏场景由责任任务恢复，用户未知变更不覆盖。

MIG63当前判断：继续迁移，尚无证据要求重写业务架构。用户已确认初验、VFXLab和Windows跑测除滚轮外正常；迁移侧补MouseManager输入归一化和冷却积累修复，Shader保持原样，2条描边警告已在2022对照复现。M1/M2独立推送，TMP84文件按N1入库并保留快照。滚轮已人工签收、Tomato双路径已验证；C/M4已完成、E1已通过用户终验并集成main；H和独立定向重序列化已完成，下一步由用户安排功能批次F（不做服务端全离线镜像）。MIG-F01为H后的小项：按Unity 6统一单位优化阈值和手感，Mac适配在Mac使用前完成。Windows有意保留D3D11优先/D3D12第二。字体提交保护与两个手动探针长期保留。公司/产品名列为E1后、存档系统落地前待办。详见[M3R反馈处理](.ai-workspace/MIG63/M3R_人工反馈处理与补漏.md)，已验收main基线保持37bb854，后续工作在content开发。

## 7. 工作区与版本留档

历史inputs/outputs、图证、DevTools修复备份与整理前文档保存到单一阶段ZIP，逐文件SHA-256校验后清理散落副本。活动队列保留历史与编号，新账本从下一轮追加。测试必须依赖固定夹具，不能依赖会被归档的任务目录。

Git提交包括当前开发成果、依赖清单、工作流工具、说明文档和Git LFS阶段包。缓存、实时调度状态、独立日审运行输出继续本地维护。过程回执写任务输出，关键文档直接维护当前结论，不再累积相互矛盾的页首进度日志。

H后待办归属：客户端配置MIG-F03、无鼠标设备容错MIG-F04、E2功能命名MIG-F05、官方AI接口评估MIG-F07均见E1/H报告；F尚未指定，2026-09-29为人工检查点，不创建自动提醒、不自动展开业务或清理副本。
